# SafeFlow Control Board

**Status date:** 5 July 2026
**Project boundary:** Simulation, education and stakeholder demonstration only. No real patient data. No live NHS deployment. No automated prescribing or escalation.

## Current status

| Field | Current position |
|---|---|
| Overall status | Strong external-facing simulation preview package; live repo status re-confirmed 5 July 2026. |
| Current product boundary | Simulation-only education/demo prototype; human-review support language only. |
| Current branch | codex/safeflow-prototype |
| Open PR | None open as of 5 July 2026 (post-merge check). |
| Closed PR | #7 "Add minimal role-aware GUI foundation", branch feature/minimal-role-aware-gui. State: CLOSED as superseded on 1 July 2026. |
| Merged PR | #29 "test(safety): add regression scans for demo surfaces" (squash merge `c0bbb8a`), #30 "docs: align public demo pack master narrative" (squash merge `29c1974`), and #31 "feat: add ward quality and safety review export" (squash merge `8e1dcbb`, SF-217), all merged 5 July 2026. Post-merge verification: `npm test` 70 files/400 tests passed, `npm run build` passed. |
| Merged PR | #13 "Content views refresh", branch ui/content-views-refresh. Rebased onto latest base, `npm test` (56 files, 338 tests) and `npm run build` passed, safety-wording scan clean, merged 1 July 2026. |
| Deployment guard | SAFEFLOW_DEPLOYMENT_APPROVED now fails closed unless the exact string `true` is present; unset or other values are logged as not approved, and local dev-server providers also refuse placeholder fallback outside allowed preview environments. |
| Primary unfinished control task | Keep this file updated after every commit, doc change, or PR event; keep the app-copy naming cleanup queued as a Mia task. |

## Completed / Done

| ID | Status | Area | Work item | Evidence |
|---|---|---|---|---|
| SF-001 | Done | Safety boundary | Simulation-only boundary established: no real patient data, no live NHS systems, no prescribing/escalation claims. | Project rule / control board |
| SF-002 | Done | Simulation signals | Pure cue engine, workspace signal snapshot storage, app wiring, and patient-panel review cues implemented. | Commit 1e888ae0632898fee488e12def8a1c1b5e9ae96f |
| SF-003 | Done | Patient panel | Simulation-only Review cues section added to patient panel. | Commit 1e888ae0632898fee488e12def8a1c1b5e9ae96f |
| SF-004 | Done | Presentation Mode | Header toggle, presentation banner, simplified flow strip, and projector-friendly styling added. | Commit f55c51b |
| SF-005 | Done | Testing | Presentation Mode enable/disable tests added and focused tests passed. | npm test reported passed |
| SF-006 | Done | Build | Project build validated after Presentation Mode work. | npm run build reported passed |
| SF-008 | Done | Infra tests | infra/aws/runSynth.js compatibility fix applied so Vitest can import reliably. | Commit f55c51b |
| SF-010 | Done | Roadmap | delivery-roadmap.md updated with completed demo features and backlog items. | Commit 10aceac |
| SF-011 | Done | Exportable report | Exportable Simulation Review Report added to demo/readiness status. | Commits 0a20cd5, 105c892, 3620953 |
| SF-102 | Done | Product concept | Patient Journey Twin v2 timeline view shipped with the minimal app/nav wiring and scenario styling. | Squash merge commit `6d98cd9` via PR [#20](https://github.com/simionolimpiu-spec/wardsafe/pull/20). |
| SF-013 | Done | Clinical safety / IG | clinical-safety-ig-readiness.md created covering simulation boundary, clinical safety, IG/data protection, human review, RBAC/audit logging, future NHS/AWS readiness, risks, open questions, and simulation-to-live gates. | Commit d00b662 |
| SF-014 | Done | Stakeholder pack | stakeholder-demo-pack.md created as a concise NHS-facing demo pack. | Commit b74ab9f |
| SF-016 | Done | Control | CONTROL.md created at repo root as the permanent control tracker. | This commit |
| SF-117 | Done | Governance | Deployment guard now fails closed and logs when SAFEFLOW_DEPLOYMENT_APPROVED is unset or not exactly `true`. | Added shared deployment approval reader plus tests; deploy approval is explicit only for the exact string `true`. |
| SF-118 | Done | UI foundation | Design token layer added and `src/styles` split into focused partials; Phase 1 shell-vs-content restyle can now begin. | Branch `ui/design-foundation` |
| SF-119 | Done | UI | Phase 1 shell restyle | Shell, navigation, presentation mode, forms, dialogs, and responsive affordances refreshed on `ui/shell-and-navigation-refresh`. | Branch `ui/shell-and-navigation-refresh` |
| SF-120 | Done | UI | Phase 1 content restyle | Applied the new token layer to board, panel, drawer, scenario, and form surfaces in the content-views refresh. | Branch `ui/content-views-refresh`; commit `e900176`. |
| SF-121 | Done | UI | Panel review-cue CSS gap | Added matching panel.css rules for the review cue stack during the content-views refresh. | Branch `ui/content-views-refresh`; commit `e900176`. |
| SF-122 | Done | ML foundation | Simulated Trend Model (simulation-risk-ml-v0) shipped with synthetic-only training data, plain-JS logistic regression, and a checked-in model artifact. | Merged via PR #9 on 1 July 2026 after rebasing onto `codex/safeflow-prototype`. |
| SF-123 | Done | Backend safety gap | Local dev-server signal/suggestion providers (`createConfiguredSignalProvider`, `createConfiguredSuggestionProvider`) now refuse to silently serve placeholder fixtures when `SAFEFLOW_ENVIRONMENT` is not an allowed preview value, matching the guard already used by the Lambda handler (`allowsSimulationPreviewFallback`). Closes the "no silent placeholder fallback in production-intent environments" acceptance criterion from the delivery roadmap's DB-read-model backlog item. | `server/signalProvider.js`, `server/suggestionProvider.js`, plus new tests in `server/signalProvider.test.js` and `server/suggestionProvider.test.js`. Authored directly (Claude), not via Codex CLI, to conserve Codex usage. |
| SF-124 | Done | UI accessibility | WCAG AA accessibility pass across current UI surfaces: focus states, contrast, ARIA, keyboard navigation. | Merge commit `46ef3c0` via PR [#17](https://github.com/simionolimpiu-spec/wardsafe/pull/17). |
| SF-125 | Done | Dashboard data layer | Chart-ready aggregation service for ward comparison, simulated trend, and cue breakdowns. | Merge commit `f83d4bb` via PR [#21](https://github.com/simionolimpiu-spec/wardsafe/pull/21). |
| SF-126 | Done | Hospital insights dashboard | Hospital insights section with ward comparison and Patient Journey Twin simulated trend charts, using the shared chart-ready dashboard data service. | Squash merge commit `ccede12` via PR [#22](https://github.com/simionolimpiu-spec/wardsafe/pull/22). |
| SF-209 | Done | Data model | Fictional patient timeline + observations + review cues. | Merge commit `91c727c` via PR [#18](https://github.com/simionolimpiu-spec/wardsafe/pull/18); synthetic-only data only. |
| SF-211 | Done | Education | Clinical education, interprofessional learning, and portable competency passport | Simulation-only learning layer with micro-learning and structured courses tagged by ward/trust/profession/topic; squash merge commit `cf1577804876428723956b52e101cf0066b660af` via PR [#23](https://github.com/simionolimpiu-spec/wardsafe/pull/23). |
| SF-212 | Done | Simulation | More scenarios + safety flags | Merge commit `2abb2a04ce891bc390a132bb045535bce84a6653` via PR [#24](https://github.com/simionolimpiu-spec/wardsafe/pull/24); fictional ward scenarios and explainable safety cues only. |
| SF-213 | Done | Safety | Heuristic cue engine (explainable 'intuition') | Merge commit `fb6a9cef22158ad02f7a5e59bc94c68238d96621` via PR [#25](https://github.com/simionolimpiu-spec/wardsafe/pull/25); deterministic cue layer with transparent explanations and human review required. |
| SF-215 | Done | Safety / UI | Scope low-potassium flag and clean top nav | Merge commit `f63591d0e77dffc17611ad8ad25ef5038011ac1e` via PR [#26](https://github.com/simionolimpiu-spec/wardsafe/pull/26); scoped lab-signal flag wording to structured review support and refreshed navigation. |
| SF-214 | Done | ML | Extend simulation-risk ML model | Merge commit `ed4e5b60d1f150c5dfe258f4f52060887c8cb8e9` via PR [#27](https://github.com/simionolimpiu-spec/wardsafe/pull/27); synthetic-only model extension remains illustrative and not clinically validated. |
| SF-216 | Done | Data model | Ward simulation database | Squash merge commit `17971040696045291281b45c4d77d1120e72a724` via PR [#28](https://github.com/simionolimpiu-spec/wardsafe/pull/28); pure ward artifact rendering split out from the SQLite writer so CI can import the database fixtures safely. |
| SF-219 | Done | Governance | DTAC v2 self-assessment (working RAG, five domains) | `docs/public-demo-pack/build-readiness/dtac-v2-self-assessment.md`; honest gap register against the Feb 2026 DTAC form. Authored directly (Claude, docs-only), 3 July 2026. |
| SF-220 | Done (docs) | Evaluation | SF-210 evaluation framework: measures M1–M8, pre/post forms, SUS, facilitator sheet, data handling, pre-registered thresholds | `docs/public-demo-pack/evaluation-framework.md`. First real session still pending — see SF-210. Authored directly (Claude, docs-only), 3 July 2026. |
| SF-221 | Done | Research | NHS integration readiness research (closes SF-205): onboarding path, relevant APIs, UK Core FHIR/SNOMED implications, CIS2 seam design, ward-state negative finding | `docs/public-demo-pack/build-readiness/nhs-integration-readiness.md`; primary-source verified against digital.nhs.uk/developer on 3 July 2026. Research only; no integration authorised. |
| SF-223 | Done (docs) | Evaluation | First evaluation-session run plan: concrete, paternity-aware run-book for SafeFlow's FIRST simulation evaluation session (3–6 nurses/students; reads cues, generates a Ward Quality & Safety Review export SF-217, completes a Competency Passport module), operationalising the SF-210/SF-220 framework. Produces usability/usefulness evidence only, not clinical evidence. | `docs/public-demo-pack/build-readiness/first-evaluation-session-plan.md` (+ strategy-pack copy outside repo). Authored directly (Claude, Oli's-codex role, docs-only), 6 July 2026. Task O8. |
| SF-224 | Done (docs) | Quality reporting | SafeFlow Quality Intelligence stakeholder one-pager: single-page, plain-English description of the Ward Quality & Safety Review export (SF-217) pitched at Band 6/7/8a users (ward managers, practice educators, matrons, clinical governance leads), with a load-bearing "what it does not do" section (no staff scoring, no escalation-pathway start, does not replace EPR/RRT/Call-for-Concern or any live clinical/quality system, simulation-only). Every claim traceable to master-narrative.md. | Squash merge commit `7e9a126` via PR [#35](https://github.com/simionolimpiu-spec/wardsafe/pull/35); one new file `docs/public-demo-pack/quality-intelligence-one-pager.md` (+23/-0), no code/CONTROL.md changes in the feature PR; diff independently checked for banned wording (clean — Optica not named). Task O9. |
| SF-225 | Done (docs) | Scope discipline | Future Discovery Register: formally parks the three out-of-scope senior-nurse-feedback ideas — patient feedback stations, ward social hub, and cross-team discharge coordination (generically named; no competitor product named) — each with why-attractive / why-out-of-scope / open-governance-questions (framed as questions) / FUTURE DISCOVERY status. Points to the external IG/DPIA-lite checklist. Holds the simulation-only boundary and gives an honest "could SafeFlow also do X?" answer without building. | Squash merge commit `abcab4c` via PR [#36](https://github.com/simionolimpiu-spec/wardsafe/pull/36); one new file `docs/public-demo-pack/future-discovery-register.md` (+74/-0), no code/CONTROL.md changes in the feature PR; diff independently checked for banned wording (clean). Task O10. |
| SF-226 | Done (docs) | Quality reporting | Verified-learning-evidence narrative: adds the approved narrative home for a future ward-level verified-learning rollup (Mia's M10) to master-narrative.md — aggregated, NON-IDENTIFYING counts of completed simulation learning modules / Competency-Passport credits presented as "ward-level learning assurance (simulation)", human-review-framed, never individual staff scoring or person-level comparison. Adds approved terms ("verified learning evidence", "ward-level learning assurance (simulation)") and banned terms ("staff scoring", "competency ranking", "performance league table", "individual nurse rating"). | Squash merge commit `1c5e523` via PR [#37](https://github.com/simionolimpiu-spec/wardsafe/pull/37); one file `docs/public-demo-pack/master-narrative.md` (+11/-1); approved terms verified in the Approved Terminology list and banned terms in the Banned Wording list (correct placement independently confirmed); no code/CONTROL.md changes in the feature PR. Task O11. |
| SF-228 | Done | Scenarios | Deterioration-pattern review scenarios (Mia, M6): 3 fictional patterns — rising respiratory rate, new-onset confusion, falling oxygen saturation — that produce explainable, simulation-only, human-review-framed review cues through the existing signal engine. Each scenario carries success signals ("stays calm and explainable", "simulation-only and human-review required") and hazard guards (avoid treatment language / live-alert framing); signalEngine gains a guard regex blocking clinical wording. | Squash merge commit `28f54f0` via PR [#38](https://github.com/simionolimpiu-spec/wardsafe/pull/38); 3 files additive (`src/data/scenarioLibrary.js`, `src/domain/signalEngine.js`, `src/domain/signalEngine.test.js`, +184/-1), no server/infra/package/CONTROL.md changes in the feature PR; diff independently checked — banned-term matches were only the scenarios' own hazard labels (what to avoid) and the new guard regex, no banned usage; both CI checks green + CLEAN. From Mia's Codex. |
| SF-227 | Done | Safety QA | Safety-language scan coverage extended to the public demo pack markdown — the controlled wording source. `safetyLanguage.test.js` DOCUMENTATION_FILES now also scans master-narrative.md, quality-intelligence-one-pager.md, future-discovery-register.md, safety-boundary.md, stakeholder-demo-pack.md, demo-script.md, and build-readiness/first-evaluation-session-plan.md, so a future wording edit in the demo pack is caught by CI (closes the gap flagged when PR #29 covered only UI components). The scan found no banned wording to fix in those docs — confirming the Quality Intelligence docs merged this session are clean. | Squash merge commit `e80e5f5` via PR [#39](https://github.com/simionolimpiu-spec/wardsafe/pull/39); 1 file `src/domain/safetyLanguage.test.js` (+7/-0), test-only; both CI checks green + CLEAN; built fresh (the conflicting `pre-merge-safety-stash-2026-07-05` was left untouched). Task = Claude/Oli-codex safety-scan extension. |
| SF-127 | Done | Safety QA | Safety-language regression scan coverage extended to Twin/Hospital Insights/newer surfaces. | Squash merge commit `c0bbb8a` via PR [#29](https://github.com/simionolimpiu-spec/wardsafe/pull/29); adds `src/domain/safetyLanguageScans.test.js` and `src/components/safetyLanguageSurfaces.test.jsx` so future fixture/copy edits on these surfaces are caught automatically. |
| SF-229 | Done | Safety QA | Safety-language regression scans extended to Reports view and Ward Safety Board surfaces (renders real components incl. Quality Review drawer, scans rendered text with boundary-aware scanner). | Squash merge commit `1084791` via PR [#43](https://github.com/simionolimpiu-spec/wardsafe/pull/43); 1 test file extended (`src/components/safetyLanguageSurfaces.test.jsx`), zero production changes; both CI checks green. From Mia's Codex; QA'd and merged by orchestrator (Claude) 7 July 2026. |
| SF-230 | Done (docs) | Evaluation | In-session feedback capture sheet (O13) — quick-capture companion to the SF-220 evaluation framework, with explicit "framework wins" precedence note and cross-links to SF-220/SF-223. | Squash merge commit `793e92a` via PR [#42](https://github.com/simionolimpiu-spec/wardsafe/pull/42); `docs/public-demo-pack/build-readiness/evaluation-feedback-capture-sheet.md`. Precedence note added at merge time to prevent dual-instrument drift (HAZ-06/R-005). |
| SF-231 | Done | Safety QA | Ward Quality & Safety Review accessibility + review hardening (M9, from Mia's Codex): test coverage locking the export drawer/service behaviour and accessible review output. | Squash merge via PR [#47](https://github.com/simionolimpiu-spec/wardsafe/pull/47); `WardQualitySafetyReviewDrawer.test.jsx` + `wardQualitySafetyReviewService.test.js`; both CI checks green; test-only, no production or safety-guard changes. Verified + merged by orchestrator (Claude) 10 July 2026. |
| SF-232 | Done | Safety QA | Deterioration-scenario explainability lock (M11, from Mia's Codex): asserts each deterioration cue's visible copy explains WHY it flagged, stays non-alarming and human-review-framed, with a guard regex disallowing action/escalation/diagnosis wording. | Squash merge via PR [#46](https://github.com/simionolimpiu-spec/wardsafe/pull/46); `src/data/scenarioLibrary.js` + `scenarioLibrary.test.js`; both CI checks green; diff independently checked (banned matches were the guard regex only, not usage). Verified + merged by orchestrator (Claude) 10 July 2026. |
| SF-233 | Done (docs) | Governance | Staged information-governance checklist (delivers SF-206): simulation / evaluation / company / pilot stages with blocking gates surfaced (visa + IP checks before incorporation; DPIA before any real data). | Squash merge via PR [#45](https://github.com/simionolimpiu-spec/wardsafe/pull/45); `docs/public-demo-pack/build-readiness/ig-checklist.md`. Formal row added 10 July 2026 to close the phantom-ID gap (previously tracked only in the SF-206 note). |
| SF-234 | Done (docs) | Demo / Docs | Public simulation-preview deployment paths — clarifies how the simulation-only static build is published/reached (aligns docs with the GitHub Pages / static-host deploy work), simulation-only, no clinical claims. | Squash merge via PR [#50](https://github.com/simionolimpiu-spec/wardsafe/pull/50); `docs/public-demo-pack/build-readiness/public-simulation-preview.md` (+24/-16); banned-word scan clean; docs-only. Renumbered from Codex's proposed SF-234 collision-check by orchestrator (Claude) 10 July 2026. |
| SF-235 | Done (docs) | Evaluation | Evaluation-session facilitation script (O14): minute-by-minute read-aloud facilitator runbook for the first simulation evaluation session — participant-only consent, simulated-data boundary, the review-cues → Ward Quality & Safety Review export → Competency Passport sequence, SF-230 capture points, and debrief. Usability/usefulness evidence only, not clinical. Cross-refs SF-223 and SF-230. | Squash merge via PR [#51](https://github.com/simionolimpiu-spec/wardsafe/pull/51); `docs/public-demo-pack/build-readiness/evaluation-session-facilitation-script.md`. Rebuilt from Codex's unpushed loose worktree work; hardcoded SF-234 corrected to SF-235; safety scan 7/7. |
| SF-236 | Done (docs) | Demo / Docs | Live-site demo walkthrough (O15): click path + plain-English narration for driving the deployed static simulation preview in front of non-clinical (e.g. graphic designer) and Band 6/7 audiences, with the on-screen simulation-only boundary locations called out. Added to the safety-language scan and cross-linked from README/demo-script/demo-readiness. | Squash merge via PR [#51](https://github.com/simionolimpiu-spec/wardsafe/pull/51); `docs/public-demo-pack/live-site-demo-walkthrough.md` + scan/cross-link edits. Boundary "does not diagnose/prescribe" language confirmed allowed by the boundary-aware scanner. |
| SF-237 | Done | Quality reporting | Ward-level verified-learning rollup (M10) — adds an AGGREGATE, NON-IDENTIFYING "Ward-level learning assurance (simulation)" section to the Ward Quality & Safety Review export (service snapshot + export text + drawer UI), sourced from the competency passport (verified-credit + points counts) and micro-learning fixtures (module counts). No individual staff names, scores, ranking, or league table (R-002). Uses approved SF-226 wording. | Squash merge commit `b89c06a` via PR [#52](https://github.com/simionolimpiu-spec/wardsafe/pull/52); 3 files (`wardQualitySafetyReviewService.js`, `WardQualitySafetyReviewDrawer.jsx`, service test, +156/-1); test asserts aggregate counts, approved wording, and that NO fixture student name leaks into the export; safety scans + build pass. Authored directly by orchestrator (Claude) via desktop-commander, 10 July 2026. |
| SF-238 | Done (docs) | Demo / Docs | Demo-pack index & reading order (O16) — `pack-index.md` groups the now-large demo pack by audience (start-here / stakeholder / evaluation / governance / build-readiness), reaffirms master-narrative.md as the controlled wording source, and is added to the safety-language scan. | Squash merge commit `e23f5a7` via PR [#53](https://github.com/simionolimpiu-spec/wardsafe/pull/53); `docs/public-demo-pack/pack-index.md` + 1 line in `safetyLanguage.test.js` DOCUMENTATION_FILES; safety scan 7/7. Authored directly by orchestrator (Claude), 10 July 2026. |
| SF-239 | Done | Demo / UX | Presentation / demo mode (M12, extends existing SF-004 Presentation Mode) — toggle-able presentation mode with larger token-based legibility and a 4-step guided flow (review cues -> Ward Quality & Safety Review export -> Competency Passport -> Hospital Insights); opens the existing export drawer on its step; simulation-only banner stays visible throughout; keyboard-accessible (aria-pressed). | Squash merge via PR [#54](https://github.com/simionolimpiu-spec/wardsafe/pull/54); 3 files (`src/App.jsx`, `src/App.test.jsx`, `src/styles/shell.css`, ~144 lines); no data/package/server/infra/CI changes; banned-word scan clean; both CI checks green. From Codex; verified + merged by orchestrator (Claude) 10 July 2026 (Codex proposed SF-237 but that was assigned to M10; renumbered to SF-239). |
| SF-240 | Done (docs) | Demo / Docs | Live demo URL wired into the pack (O17) — README, pack-index, live-site-demo-walkthrough, and public-simulation-preview now reference the live public build https://simionolimpiu-spec.github.io/wardsafe/ (each with a simulation-only / not-for-clinical-use note), and record that it auto-deploys on push to codex/safeflow-prototype. | Squash merge via PR [#55](https://github.com/simionolimpiu-spec/wardsafe/pull/55); 4 docs (+13/-1); banned-word scan clean; both CI checks green. Live site verified HTTP 200 with JS bundle loading before wiring. From Codex; verified + merged by orchestrator (Claude) 12 July 2026. |
| SF-241 | Done | Data / Simulation | Two-trust simulation patient database (`src/data/trustNetwork/`). Real trust + ward NAMES only (James Paget = EXACT 33-ward directory from jpaget.nhs.uk, July 2026; Norfolk & Norwich = sourced 18-ward subset from nnuh.nhs.uk Wards A-Z, July 2026, flagged to re-verify); all PATIENTS and journeys FICTIONAL and non-identifying. Models inter-trust handover: James Paget -> Norfolk & Norwich specialist care -> health-plan continuation -> return to James Paget OR packages of care / discharge (4 journeys, 20 segments). Simulation-only, human-review-framed; NOT a live inter-trust record or coordination system. FK-integrity-checked; CLI generator emits CSV + manifest (no node:sqlite). | Squash merge commit `b515b89` via PR [#57](https://github.com/simionolimpiu-spec/wardsafe/pull/57); new module `trusts.js`/`index.js`/`trustNetworkArtifacts.js`/`trustNetwork.test.js` + `scripts/build-trust-db.mjs` + generated `data/exports/two-trust/*`; 7 module tests + 17 safety-scan tests + build all green; additive, not yet wired into UI (proposed follow-up). Authored directly by orchestrator (Claude), 12 July 2026. |
| SF-242 | Done | Data / UX | Two-Trust Network view — read-only UI (`src/components/TrustNetworkView.jsx`) rendering the SF-241 data on the live site: the two trusts with ward + fictional-patient counts, and the inter-trust handover journeys (trust chips + ward + stage + handover note + outcome badge) so the handover between James Paget and Norfolk & Norwich and the continuing health plan are visible. Simulation-only banner unchanged; real ward names, fictional patients, not a live inter-trust system. | Squash merge commit `2162542` via PR [#58](https://github.com/simionolimpiu-spec/wardsafe/pull/58); new view + test + `trust-network.css`, a 'Two-Trust Network' tab + render branch in `App.jsx`, added to the boundary-safe safety-language surface scan; 69/69 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-243 | Done | Data / Simulation | England trust network — expands SF-241 from 2 to **6 trusts**: James Paget (EXACT 33-ward directory), Norfolk & Norwich + Addenbrooke's/CUH (sourced real wards, inc. NNUH Buxton/Cringleford/Holt AMU and CUH neurosciences A3-A5 / transplant F5/G5), plus West Suffolk, Ipswich (ESNEFT) and QEH King's Lynn (representative real FUNCTIONAL units, labelled per-trust via `wardSource`). 99 wards, 209 fictional non-identifying patients (each with a `homeTrustId`). Simulation-only. | Squash merge commit `366f92d` via PR [#59](https://github.com/simionolimpiu-spec/wardsafe/pull/59); rewrote `trusts.js`/`index.js`/artifacts + regenerated `data/exports/two-trust/*` (now inc. `learning_records`); FK-integrity OK; 75/75 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. NNUH/representative ward completeness flagged to re-verify. |
| SF-244 | Done | Data / UX / Governance | Portable cross-trust patient journey + **learning-copy-to-origin** — patients carry a home trust; journeys model relocation / temporary-visitor (found away from home) / specialist-transfer / inter-trust-handover with the health plan continuing and the patient returning home or moving to packages of care / discharge; every journey emits a `learningRecord` returned to the ORIGINATING trust with teaching points (the teaching feedback loop). Concept + honest IG boundary in `portable-patient-journey-concept.md` (live version = regulated Shared Care Record / DPIA future discovery, NOT a live record). Trust Network view + generated CSVs render it. | Squash merge via PR [#59](https://github.com/simionolimpiu-spec/wardsafe/pull/59); view/test updates, concept doc added to safety scan, `learning_records` CSV table; boundary-safe surface scan + 75/75 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-245 | Done | Data / UX | Per-patient fictional observation timelines (`patientTimelines.js`) — each of the 209 fictional trust-network patients gets a deterministic 4-point NEWS2-style sequence (resp/SpO2/HR/systolic BP/temp/consciousness) seeded from its id; drifts only for observation-trend / escalation-readiness themes (explainable review cue), otherwise stable; trend note is review-support framing, not a clinical score. Journeys carry a fictional patient subject; the Trust Network view renders that patient's moving observation strip in each journey card. New `observations` CSV table (836 obs). Concept doc gains an 'Alignment, not replacement' section (aligns with Shared Care Record / GP Connect, not a duplicate). | Squash merge commit `30423dd` via PR [#60](https://github.com/simionolimpiu-spec/wardsafe/pull/60); `patientTimelines.js`+test, index/artifacts/view updates, css; 23/23 tests + generator + build green; FK-integrity OK. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-246 | Done | Data / Simulation | Real sourced ward sets for the three district generals — replaces the representative placeholders: **West Suffolk** (F7/F9/F10, G3/G4/G8, E17 + ED/AMU/ICU), **Ipswich/ESNEFT** (27 real wards from the full esneft.nhs.uk Ipswich Wards A-Z: Brantham, Brook, Sproughton, Bergholt … Woodbridge, Framlingham NICU), **QEH King's Lynn** (Windsor/Necton/Elm + Tilney/Gayton/Denver/… + Castleacre maternity). Five of six trusts now carry real ward names (James Paget exact; the rest sourced). Cross-region journey ward ids updated to the new real wards. Now 126 wards, 293 fictional patients, 1,172 observations. | Squash merge commit `4bcc12d` via PR [#61](https://github.com/simionolimpiu-spec/wardsafe/pull/61); rewrote WSH/ESNEFT/QEH ward arrays in `trusts.js`, wardSource representative→sourced, updated journey segment ids in `index.js`, regenerated exports; 10/10 tests + generator + build green; FK-integrity OK. Authored directly by orchestrator (Claude), 12 July 2026. Each trust flagged partial/re-verify for full completeness. |

## Started / Open / Ready

| ID | Status | Area | Work item | Next action |
|---|---|---|---|---|
| SF-101 | Closed | Release | PR #7 feature/minimal-role-aware-gui closed as superseded. | No merge planned. |
| SF-104 | Done | AWS architecture | Mock/readiness AWS direction documented in `docs/public-demo-pack/build-readiness/architecture-options.md` and `docs/public-demo-pack/build-readiness/technology-stack.md` (Aurora, Step Functions, Bedrock/LLM draft provider, tokenised backend, RBAC/audit logging). | Merged via [PR #19](https://github.com/simionolimpiu-spec/wardsafe/pull/19) on 2 July 2026; squash merge commit `5384b54b7f71d61b9b614df7c753255edab39d18`. Keep as mock/readiness architecture only; no live AWS deployment or SDK wiring until explicit approval. |
| SF-105 | Done | Docs alignment | `docs/public-demo-pack/master-narrative.md` is now the controlled description; `stakeholder-demo-pack.md`, `demo-script.md`, `safety-boundary.md`, and `README.md` are aligned to it. | Merged via PR [#44](https://github.com/simionolimpiu-spec/wardsafe/pull/44), squash commit `f7c918f`, 7 July 2026 (conflicts with #37/#40/#41 resolved in favour of newer base wording — the QI-lane master-narrative with SF-226 terms is the surviving version). App-copy naming cleanup remains a Mia follow-up. |
| SF-107 | Done | Clinical safety | Simulation twin / learning idea consolidated as a bounded, simulation-only, review-support + education concept (fictional Patient Journey Twin over fictional timelines feeding the learning / Competency-Passport loop; never a real-patient twin, predictor, or clinical action). | Concept recorded in `docs/public-demo-pack/build-readiness/simulation-twin-learning-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)); review-support and education only. |
| SF-109 | Done | Governance | Clinical safety case outline and hazard log skeleton drafted. | `docs/public-demo-pack/clinical-safety-case-outline.md`, using `templates/safety-case-outline-template.md`; 10-row hazard log grounded in the actual signal-envelope contract (`signalClient.js`) and existing risks R-001â€“R-008. Not yet independently reviewed; roles unfilled pending a future pilot. |

## Quality Intelligence lane (5 July 2026, from senior-nurse feedback pressure-test)

Senior-nurse feedback (Band 6/7 quality reports, deteriorating-patient review, RRT/Call-for-Concern, patient feedback stations, social hub, Optica-style discharge coordination) was pressure-tested against the simulation-only boundary; full memo saved outside this repo as `SafeFlow_Senior_Nurse_Feedback_Strategy_Memo_2026-07-05.md`. Verdict: patient feedback stations and a social hub stay out as future discovery (real IG/consent gap, SF-206 still backlog); Optica-style discharge coordination and naming a real competitor product stay out of external material entirely. The two in-scope items below are dispatched as SF-217 (Oli) and SF-218 (Mia).

| ID | Status | Area | Work item | Next action |
|---|---|---|---|---|
| SF-217 | Done | Quality reporting | Ward Quality & Safety Review export — assembles heuristic cue engine flags, simulation-risk trend summary, and Competency Passport verified-learning evidence into one Band-6/7-styled exportable report, reusing the existing Simulation Review Report export pattern. | Squash merge commit `8e1dcbb` via PR [#31](https://github.com/simionolimpiu-spec/wardsafe/pull/31); 9 files (`src/App.jsx`, `src/App.test.jsx`, `src/components/ReportsView.jsx`, new `WardQualitySafetyReviewDrawer.jsx`+test, extended `safetyLanguageSurfaces.test.jsx`/`safetyLanguageScans.test.js`, new `wardQualitySafetyReviewService.js`+test); post-merge `npm test` 70 files/400 tests passed, `npm run build` passed; diff independently checked for banned wording (clean — only approved terms like "escalation readiness cue" and the standard boundary phrase appear). No server/infra/package.json/CONTROL.md changes. |
| SF-218 | Done | Education | Deterioration + escalation-pathway learning modules — 2–3 new Learning Hub micro-learning modules covering deteriorating-patient recognition and RRT-call/Call-for-Concern conversation structure, education only, no live escalation logic. | Squash merge via PR [#48](https://github.com/simionolimpiu-spec/wardsafe/pull/48) (M7, from Mia's Codex); adds `src/data/microLearningFixtures.js` content + LearningHubView/drawer/service/safety-scan tests; both CI checks green; diff independently checked (no banned usage — only guard patterns). Verified + merged by orchestrator (Claude) 10 July 2026. |

## Backlog

| ID | Status | Area | Work item | Notes |
|---|---|---|---|---|
| SF-201 | Done | UI | Full-screen / presentation mode — delivered by the M12 presentation mode (SF-239): toggle-able presentation mode with larger token-based legibility, a 4-step guided flow, and the simulation-only banner always visible. | Closed 12 July 2026 as delivered by SF-239 (PR [#54](https://github.com/simionolimpiu-spec/wardsafe/pull/54)). Any further full-screen polish is optional and would land as additive token CSS. |
| SF-202 | Done | UI | Cleaner NHS-realistic v2 visual concept — calm / clinical-grade / accessible design direction (colour roles, type & spacing, component notes) for a graphic designer to build on; token-driven, additive, no behaviour or boundary change. | Concept in `docs/public-demo-pack/build-readiness/nhs-realistic-v2-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Any resulting restyle lands as additive token CSS after designer input. |
| SF-204 | Closed (superseded) | AWS docs | AWS mock docs and architecture diagrams | Delivered by SF-104 (PR #19, `docs/public-demo-pack/build-readiness/architecture-options.md` + `technology-stack.md`). No further action. |
| SF-205 | Closed (delivered) | Research | NHS Digital API research only | Delivered by SF-221 (`nhs-integration-readiness.md`). |
| SF-206 | Closed (delivered) | Governance | IG checklist | Delivered by SF-233 (`docs/public-demo-pack/build-readiness/ig-checklist.md`, PR [#45](https://github.com/simionolimpiu-spec/wardsafe/pull/45)): staged checklist (simulation / evaluation / company / pilot) with blocking gates surfaced (visa + IP checks before incorporation; DPIA before any real data). SF-231/SF-232 reserved for Mia's pending branches; SF-218 fulfilled by her escalation-pathway-learning branch when it lands. |
| SF-207 | Done | Security | RBAC + audit-logging concept/readiness — least-privilege role model + append-only, tamper-evident audit design for a FUTURE governed pilot only; nothing built or enabled now (no real data, R-003). | Concept in `docs/public-demo-pack/build-readiness/rbac-audit-logging-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Downstream of the clinical safety case + IG checklist; recorded as design/readiness, not authorisation to implement. |
| SF-210 | Ready to run | Evaluation | Demo evaluation framework — the full kit is complete and consistent (framework SF-220, run plan SF-223, facilitator script SF-235, capture sheet SF-230, plus a live demo to evaluate). | Readiness summary in `docs/public-demo-pack/build-readiness/evaluation-readiness-summary.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Only remaining step is operational: schedule + recruit 3–6 participants, run the session, write the first report in `docs/evaluation-reports/`. |

## Risks and controls

| ID | Risk | Control |
|---|---|---|
| R-001 | Project sounds like a live clinical decision tool. | Use simulation-only, education, human review, and review-support wording everywhere. |
| R-002 | Scope creep into prescribing, treatment advice, or automated escalation. | Avoid live-clinical action claims and keep the review-support boundary explicit. |
| R-003 | AWS deploy risk or accidental cost. | Keep SAFEFLOW_DEPLOYMENT_APPROVED false (explicitly set, not merely unset) unless explicitly preparing controlled simulation deployment. |
| R-004 | Real patient data accidentally enters demo material. | Use fictional patients, fictional observations, and sanitised examples only. |
| R-005 | Docs, slides, and app language drift apart. | Update this control board after each change; treat exported chat docs as history, not live truth. |
| R-006 | Public preview shared before release review. | Confirm PR status, CI, branch cleanliness, and wording before merge/tag. |
| R-007 | LLM/digital twin wording creates unsafe expectations. | Call it Patient Journey Twin / Simulation Patient Twin and explain that it supports learning and review, not autonomous care. |
| R-008 | Control docs reference the wrong PR number after external export/import. | Always re-pull live git/GitHub state before updating this file; do not trust prior chat summaries alone. |
| R-009 | Simulated ML trend output (`patientJourneyTrendModel.js`, weights in `patientJourneyTrendModel.json`) gets read as a validated clinical result. | Keep "Illustrative model output, not clinically validated" on every rendered trend chart/summary; never surface `testMetrics` (accuracy/precision/recall) in the UI â€” confirmed today they are data-layer only, not rendered. See SF-127 for closing the automated-scan gap on this and the Twin/Hospital Insights surfaces. |
| R-010 | Next-phase scope drifts into live or black-box claims. | Keep this phase simulation-only, with no real-patient data, no autonomous clinical action, no AGI/black-box language, and ship each item as its own scoped PR. |

## Core decisions to preserve

- SafeFlow / WardSafe remains simulation-only unless governance and deployment approval change.
- No real patient data is used in the demo or project materials.
- No live NHS integrations are included at this stage.
- No AWS SDK/live deployment should occur unless explicitly approved.
- All clinical language must remain human-review focused.
- Digital twin language should be controlled as Patient Journey Twin or Simulation Patient Twin.
- This file (CONTROL.md) is now the source of truth for what has been done, what is open, and what happens next; update it after every commit, doc change, or PR event.

## Recommended next actions

| Priority | Action |
|---|---|
| 1 | Record PR #7 as closed as superseded and keep the tracker aligned with GitHub. (Done) |
| 2 | Draft a clinical safety case outline and hazard log skeleton. (Done â€” see SF-109) |
| 3 | Keep the public docs aligned with the master narrative and raise the remaining app-copy naming cleanup as a Mia task. |
| 4 | Decide the next build focus: Patient Journey Twin UI, AWS mock architecture, or full-screen presentation polish. |
| 5 | Get the clinical safety case outline (SF-109) independently reviewed once a clinical safety lead is identified. |
| 6 | Keep this file updated after every commit, document, or design decision. |

## Changelog

- ID reconciliation note: SF-217 stayed with the Ward Quality & Safety Review export; the governance docs moved SF-217→SF-219, SF-218→SF-220, and SF-219→SF-221. The doc-only SNOMED/FHIR placeholder was advanced to SF-222 so it stays clear of the new board IDs.

## Authoring note (1 July 2026)

This safety-case work (SF-109) and the roadmap refresh were authored directly against the
repository files rather than via a Codex CLI session, to conserve Codex usage for code changes
that need build/test verification. Docs-only changes like this one carry no build risk, so direct
authoring plus a short terminal-only commit/push/PR step is the lighter-weight path going forward
for non-code control-board and documentation work.

## PR #14 scope-creep finding (1 July 2026, for Mia)

PR #14 (`patient-journey-twin`) is **not ready to merge**. A disposable, unpushed local rebase
onto the latest `origin/codex/safeflow-prototype` was used to separate genuine branch content
from stale-base noise. After rebasing, the branch still shows 88 changed files and roughly
15.6k/5.8k line insertions/deletions against current base, and the expected `WorkspaceNav.jsx`
wiring change is not present at all. This is real scope creep, not a rebase artifact. Recommended
fix: start a clean branch from current `codex/safeflow-prototype` and re-apply just the new
Patient Journey Twin component(s), their domain/test files, and the intended `App.jsx` /
`WorkspaceNav.jsx` wiring â€” nothing else. See SF-102 above for the full file list this ruled out.
| SF-247 | Done | UX | Ward-first demo scenario selector (M13) - replaces the flat combinatorial "Ward x review type" dropdown with two-step selection: a **Ward** select (Gastro, AMU, Day Care, Surgical, Paediatrics, Community, General Medical, Acute Medical - derived from the ward library, not hard-coded) plus a short **Review focus** select (Documentation, Discharge readiness, Escalation readiness, Medicine timing, Observation trend, .). New `getWardOptions()` / `getReviewFocusOptions(wardId)` / `resolveScenarioId(wardId, focusId)` helpers in `wardLibrary/index.js` and `getDemoScenarioSelectionOptions()` in `demoScenarios.js`; every existing scenario id round-trips unchanged, so downstream scenario data is untouched. Simulation-only note kept on both selects. | Squash merge commit `cdcd950` via PR [#62](https://github.com/simionolimpiu-spec/wardsafe/pull/62); DemoScenarioSelector rework + component test, wardLibrary + demoScenarios helpers + tests, App wiring; CI green (both checks); safety grep clean. Built by Codex (Mia lane) from the 17 Jul dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-248 | Done | UX / Design | O18a app shell redesign - the approved "SafeFlow Nursing / Ward Safety Board" visual direction applied app-wide: deep-blue top bar with plain blue **SF logomark (deliberately NOT the NHS logo - trademark boundary)**, "SafeFlow Nursing" + view subtitle, ward-first scenario selector + steppable simulation date in the header, notification bell + "Fictional Nurse - Charge Nurse" chip; white left sidebar (`WorkspaceNav`) with icons + Tasks/Escalations badges covering all 16 views + a "Safety first - report concern" card; KPI summary-card row (`BoardSummaryCards`: patients / escalations / NEWS2>=5 / handover % / discharge-ready) computed from existing simulation data; new design tokens (`--sf-blue` family, WCAG-AA risk colours) applied across all view stylesheets. Simulation-only banner kept on every view (Hospital Insights renders its own). No realistic names/DOB/NHS-number fields introduced. | Squash merge commit `9adaf34` via PR [#63](https://github.com/simionolimpiu-spec/wardsafe/pull/63); AppShell/WorkspaceNav/BoardSummaryCards/SafetyBanner + tests, 13 stylesheets, App/e2e updates. Initial CI failure (e2e asserted the old `.metric-grid` layout) fixed by orchestrator: merged M13 into the branch, made the top bar host the ward-first DemoScenarioSelector, dropped the redundant WardContextSelector, updated the e2e journey; then CI green (both checks) + local vitest 432 + Playwright 2/2. Built by Codex (Oli lane), reconciled + merged by orchestrator (Claude), 17 July 2026. |
| SF-249 | Done | UX / Design | O18b board table + detail panel restyle - the Ward Safety Board patient table restyled to the approved direction (uppercase column headers, row hover/selected states with blue accent, risk chips readable as text + colour, link-button styling) and the `PatientSafetyPanel` right-hand detail drawer restyled with tabbed SBAR / Tasks / Audit sections per the mock, keeping the existing human-review wording (no "escalate now"-style automated language added). Safety-language surface scan extended over the restyled components. | Squash merge commit `14efb62` via PR [#64](https://github.com/simionolimpiu-spec/wardsafe/pull/64); WardSafetyBoard + PatientSafetyPanel + tests, board/panel css. Orchestrator merged the O18a-squash base into the stacked branch (kept patient-table additions, took base for shell files), retargeted the PR base, local vitest 433 + Playwright 2/2 + CI green before merge. Dead-code note: the stack resurrected the unused WardContextSelector files; deleted in the SF-247..249 records commit. Built by Codex (Oli lane), reconciled + merged by orchestrator (Claude), 17 July 2026. |
| SF-251 | Done | Data / Domain | Longitudinal patient journey engine (L1) - day-indexed deterministic model on top of the SF-245 seeding pattern: `getPatientDay(patientId, dayNumber)` (pure, cost-independent of day number - day 1300 as cheap as day 1), `buildEpisodes(patientId, throughDay)` (admission -> ward moves -> inter-trust transfer -> discharge -> community -> possible readmission, reusing the SF-243 portable-journey definitions), `compareDays(patientId, dayA, dayB)` (then-vs-now delta: per-observation change, location change, phase change, review-support-framed trendNote). Additive `buildLongitudinalPatientTimeline()` bridge into the existing Twin data source; current fixtures/tests untouched. | Squash merge commit `6c4dec3` via PR [#65](https://github.com/simionolimpiu-spec/wardsafe/pull/65); `longitudinalJourney.js`+test, `patientTimeline.js` bridge; determinism incl. day 1300, episode ordering, compareDays symmetry, perf sanity, no name/DOB/nhsNumber, banned-wording scan all covered; CI green (both checks). Built by Codex (Mia lane) from the 17 Jul longitudinal-phase dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-252 | Done | UX | Patient Journey Twin longitudinal scrubber + then-vs-now (L2) - Twin gains a day scrubber (accessible slider + prev/next, `aria-valuetext` "Day N of M", supports long journeys e.g. day 1300 without label breakage), an episode chapter strip (admission/ward/transfer/discharge/community/readmission - clicking jumps the scrubber, inter-trust segments use the Trust Network chip style), a "then vs now" panel driven by `compareDays()` (per-observation delta rows with direction + plain-language review-support note, location then/now, phase then/now), and an inline SVG trend sparkline per observation (no chart library, colour-blind-safe, values also readable as text). Default view = latest day; selected day persists across patients only while valid. | Squash merge commit `3839d6f` via PR [#66](https://github.com/simionolimpiu-spec/wardsafe/pull/66), stacked directly on the SF-251 commit (identical engine file, zero-diff on shared files - verified before merge); `PatientJourneyTwin.jsx` rework + test, `scenario-and-audit.css` additions, safety-surface scan extended. CI green (both checks); local re-verification post-merge: 441/441 vitest, Playwright 2/2, build green. Built by Codex (Oli lane), verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-250 | Done | UX / Design | Visual fidelity pass on the O18 redesign - fixed the root cause of the font mismatch (tokens.css declared Inter but nothing ever loaded it; self-hosted `@fontsource/inter` 400/500/600/700/800 now imported in `main.jsx`, e2e now asserts computed `font-family` actually contains Inter); fixed the date stepper (previously changed the visible label but left `<time dateTime="2026-06-17">` hardcoded - now tracks the real displayed date via `toDateTimeValue()`); replaced the dead notification bell with a working "Simulation notifications" popover (honest "No simulation notifications recorded" placeholder, proper aria-expanded/aria-controls); removed the duplicate navigation (the old horizontal `tab-list`/"Prototype journey" strip that still drove view-switching underneath the new sidebar) so WorkspaceNav is the single source of navigation, with `e2e/safeflow.spec.js` rewritten to drive all views through the sidebar (all prior safety assertions - banner, no "NHS" text, potassium safety-gap flow, SBAR draft save, scenario library - kept intact); removed dead `.metric-grid` CSS. Orchestrator note: a suspected "NEWS2 ≥5" encoding bug flagged in the dispatch prompt was investigated post-PR and found to be a false positive (PowerShell console codepage artefact, not a file corruption) - confirmed via direct Node codepoint read (U+2265, correct); no fix was needed or applied, and the label was left untouched. | Squash merge commit `f9f330e` via PR [#67](https://github.com/simionolimpiu-spec/wardsafe/pull/67); `AppShell`+test, `App.jsx`+test, `WardSafetyBoard.jsx`, `main.jsx`, board/forms-and-dialogs/responsive/shell css, `package.json` (+@fontsource/inter), e2e rewrite. CI green (both checks); local re-verification post-merge: 443/443 vitest, Playwright 2/2 (desktop+mobile), build green, safety grep clean (no nhsNumber/DOB/NHS-No/NHS-logo). Built by Codex (Oli lane) from the 17 Jul fidelity-pass dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |

| SF-253 | Done | Data / Infra | AWS/DB longitudinal scale-up (L3) - `journey_episodes` and `longitudinal_observations` tables added to `database/schema.sql`, keyed on `(patient_id, day_number)` with 4 range-query indexes; deliberately not FK'd into `patient_summaries` (that table uses the older `DCU-XXX` ward-simulation namespace, these use the SF-243/245 trust-network `JPUH-P-001`-style namespace - documented in SQL comments to prevent future conflation). `src/domain/longitudinalMaterialization.js` (pure row/CSV mapping) + CLI-only `scripts/build-longitudinal-db.mjs` (SF-216 lesson: no vitest/node:sqlite contamination) materialise the SF-251 engine's output at scale: 293 patients x 1300 days = 380,900 patient-days / 2,285,400 observation rows in 27.2s (~84k rows/sec) - measured as generation+serialisation only, since no live Postgres instance is reachable in this dev/CI environment; live query-latency proof against a provisioned database is deferred to the existing `SAFEFLOW_DEPLOYMENT_APPROVED` gate (R-003), not fabricated here. Three new read-only GET endpoints (`/api/simulation/longitudinal/patients/:id/days/:day`, `/episodes`, `/compare`) added identically to `server/api.js` and `infra/aws/lambda/safeflowApi/index.mjs` - no DB round-trip needed since the engine is deterministic and O(1) per day. `database/migrationApproval.json` updated (schema hash) through the existing human-gate migration runner, no bypass. Committed `manifest.json` reflects the smaller 60-day default sample (17,580 patient-days / 105,480 observations); large CSV exports gitignored. | Squash merge commit `0072475` via PR [#68](https://github.com/simionolimpiu-spec/wardsafe/pull/68); `database/schema.sql`+test, `migrationApproval.json`, `longitudinalMaterialization.js`+test, `build-longitudinal-db.mjs`, `server/api.js`+test, `infra/aws/lambda/safeflowApi/index.mjs`+test, `.gitignore`, `package.json` scripts, `data/exports/longitudinal/manifest.json`. CI green (both checks); local re-verification post-merge: 79/79 vitest files, 460/460 tests, build green, `db:manifest`/`db:migrate:plan` match approval, `infra:synth:dev`/`infra:synth:simulation` clean, `api:smoke` 7/7, Playwright 2/2, safety grep clean (no nhsNumber/DOB/NHS-No/name). Built and merged directly by orchestrator (Claude), 17 July 2026. |

| SF-254 | Done | Governance / IG | Longitudinal-record IG boundary doc (L4) - `docs/public-demo-pack/build-readiness/longitudinal-record-boundary.md`, mirroring the structure of the SF-245 portable-journey concept doc: distinguishes the simulated/generated longitudinal data built in SF-251/252/253 from what a future LIVE captured longitudinal record would require - a DPIA scoped specifically to longitudinal (not single-snapshot) processing, Article 6/9 lawful basis kept current as the record grows, a DCB0129/0160 safety-case rescope for trend-over-time review (staleness/drift/over-trust-in-an-old-trend failure modes a single-snapshot safety case doesn't cover), DSPT currency, per-trust data-sharing agreements, and a retention section specific to long records (per-data-point retention timers, partial erasure/rectification mid-record, per-time-slice treatment of the existing learning-copy pattern). Same "alignment, not replacement" posture vs Shared Care Record/GP Connect as SF-245. Live longitudinal capture stays future discovery, gated behind this doc plus `ig-checklist.md`. Doc added to the safety-language scan file list (`safetyLanguage.test.js`); passed cleanly on first run. | Commit `fe900c5` on `codex/safeflow-prototype` (docs-only, direct); new doc + `safetyLanguage.test.js` DOCUMENTATION_FILES addition. Local verification: 79/79 vitest files, 460/460 tests (incl. the extended safety-language scan), build green, diff grep clean (no nhsNumber/DOB/NHS-No/competitor names/automated-escalation wording). Written and pushed directly by orchestrator (Claude), 17 July 2026. |

| SF-255 | Done | Data / UX | Ward-level longitudinal rollup (Trust Network) - new `src/domain/wardLongitudinalRollup.js`: `buildWardLongitudinalRollup(wardId, dayNumber)` averages every trust-network patient on a ward (via `getPatientsForWard`) through the existing SF-251 engine (`getPatientDay`), returning per-observation averages + a `reviewFlagCount` that only counts the engine's own existing `reviewThemeCue` field (no new scoring/thresholds invented); `compareWardDays(wardId, dayA, dayB)` gives a ward-level then-vs-now delta with a review-support-framed `trendNote` (never a clinical judgement), matching `compareDays()`'s tone. Deterministic and cheap regardless of day number, same property as the underlying engine. `TrustNetworkView.jsx` gains a compact "Ward trend (simulation)" panel per ward comparing day 1 vs day 90 cohort averages. Explicitly scoped to the trust-network patient universe only (`JPUH-P-xxx`-style IDs) - does NOT touch the older `wardLibrary`/`patient_summaries` (`DCU-XXX`) namespace used by Hospital Insights/Reports, preserving the SF-253 architectural separation. | Squash merge commit `cc95b76` via PR [#69](https://github.com/simionolimpiu-spec/wardsafe/pull/69); `wardLongitudinalRollup.js`+test, `TrustNetworkView.jsx`+test, `safetyLanguageSurfaces.test.jsx` extended, `trust-network.css`. Built by Codex (Oli lane, driven live via computer-use by the orchestrator), independently verified before merge: diff scoped to exactly 6 files (no wardLibrary/patient_summaries/DCU- touched), safety grep clean, both CI checks green (CLEAN), local re-verification post-merge: 80/80 vitest files, 466/466 tests, build green. Verified + merged by orchestrator (Claude), 20 July 2026. |

| SF-256 | Done | UX | Interactive day-range picker for ward trend - the SF-255 "Ward trend (simulation)" panel on `TrustNetworkView.jsx` compared a hardcoded Day 1 vs Day 90 pair; this makes it user-selectable per ward via a preset dropdown (Day 1 vs Day 30 / 90 [default, unchanged] / 180 / 365 / 1300), reactively recomputing `compareWardDays(ward.id, dayA, dayB)` on selection. An unrecognised/empty selection falls back to the original Day 1 vs Day 90 default, so nothing changes for a user who never touches the control. Deliberately did not touch `src/domain/wardLongitudinalRollup.js` or `src/domain/longitudinalJourney.js` - this is a UI-only change to which two days get compared, not to the rollup/engine logic itself. | Squash merge commit `d2229a2` via PR [#70](https://github.com/simionolimpiu-spec/wardsafe/pull/70); `TrustNetworkView.jsx`+test, `trust-network.css`. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-caught and fixed one flaky test assertion (respiratory-rate display coincidentally identical at day 30 vs day 90 for the test ward - switched the second comparison point to day 180) before reporting. Independently verified before merge: diff scoped to exactly 3 files, no `wardLongitudinalRollup.js`/`longitudinalJourney.js`/`wardLibrary` changes, safety grep clean, both CI checks green (CLEAN, opened ready-for-review not draft this time), local re-verification post-merge: 80/80 vitest files, 467/467 tests, build green. Verified + merged by orchestrator (Claude), 20 July 2026. |

| SF-257 | Done | Data / Infra | Ward rollup read-only API endpoints - gives the SF-255 ward-cohort rollup (`buildWardLongitudinalRollup`/`compareWardDays` in `src/domain/wardLongitudinalRollup.js`) API parity with the existing patient-level longitudinal endpoints (L3/SF-253): two new read-only GET routes, added identically to `server/api.js` and `infra/aws/lambda/safeflowApi/index.mjs` - `/api/simulation/longitudinal/wards/:id/rollup?day=` and `/api/simulation/longitudinal/wards/:id/compare?from=&to=` - mirroring the existing patient-route regex/handler/response-envelope pattern exactly, source label `ward-longitudinal-rollup-engine`. No database round-trip (same O(1)-per-day property as the underlying engine); no server/domain/UI files outside the API layer touched. | Squash merge commit `ab6583f` via PR [#71](https://github.com/simionolimpiu-spec/wardsafe/pull/71); `server/api.js`+test, `infra/aws/lambda/safeflowApi/index.mjs`+test. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-diagnosed a full-suite timeout as a reporter-pipe/duration issue (not a real failure) and correctly reran with a longer window rather than "fixing" a non-bug. Independently verified before merge: diff scoped to exactly the 4 expected files, safety grep clean, both CI checks green (CLEAN, ready-for-review not draft), local re-verification post-merge: 80/80 vitest files, 475/475 tests, build green, `api:smoke` 7/7, `infra:synth:dev` Lambda bundles clean (includes the new routes). Verified + merged by orchestrator (Claude), 20 July 2026. |

| SF-258 | Done | Testing / Quality | Trust Network e2e coverage - the Playwright suite (`e2e/safeflow.spec.js`) had zero coverage of the Trust Network view despite three recent PRs (SF-255/256/257) adding real interactive behavior there. Added a scoped block to the existing safety-boundary journey test: navigates via `workspaceNav.getByRole('button', { name: 'Trust Network', exact: true })`, asserts the `England Trust Network (simulation)` region/heading render, asserts the simulation safety-boundary banner and "Simulation only" text stay visible on this view, asserts a ward's "Ward trend (simulation)" panel (SF-255) is visible with its averages table and fictional-cohort count, then exercises the SF-256 day-range picker end-to-end in a real browser: selects a different preset (`1-180`) and asserts the displayed range label and the respiratory-rate row value both actually change from their Day 1 -> 90 default - proving the picker is wired to the rollup engine, not just unit-tested. | Squash merge commit `4e51315` via PR [#72](https://github.com/simionolimpiu-spec/wardsafe/pull/72); `e2e/safeflow.spec.js` only (+20/-0), no other files touched. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-diagnosed and correctly resolved two environment red herrings without help - a pre-existing 60s-per-test Vitest timeout on the render-heavy `TrustNetworkView.test.jsx` (isolated and reran with an explicit longer timeout, confirmed not a functional failure) and a Windows `EPERM` node_modules race from its own parallel validation batch (stopped the colliding jobs, reran serially with npm). Independently verified before merge: `git diff` against base confirmed the pushed branch was exactly the single e2e file (+20/-0) - the Codex UI's file-change panel had transiently shown unrelated `pnpm-lock.yaml`/`pnpm-workspace.yaml` deletions from its own cleanup of a failed concurrent `pnpm install`, which the actual pushed diff did not contain; safety grep clean; both CI checks green (ready-for-review, not draft); local re-verification post-merge: Playwright 2/2 (desktop + mobile Chromium). Verified + merged by orchestrator (Claude), 20 July 2026. |

| SF-259 | Done | Testing / Quality | Remaining nav e2e coverage - closes out the last gap in `e2e/safeflow.spec.js`'s WorkspaceNav coverage. Added the four destinations that still had zero e2e coverage after SF-258 (Trust Network) - Hospital insights, Portable Competency Passport, Learning Hub, and Patient Journey Twin - as new entries in the existing `destinations` array, using each view's real rendered heading (read directly from the component source, not guessed) so every WorkspaceNav destination except the two already covered by dedicated tests (Scenarios, Trust Network) is now exercised by the same click-and-assert-heading loop, keeping the existing simulation-safety-boundary assertions intact across every view. | Squash merge commit `320e73f` via PR [#73](https://github.com/simionolimpiu-spec/wardsafe/pull/73); `e2e/safeflow.spec.js` only (+4/-0), no other files touched. Built by Codex (Oli lane, driven live via computer-use by the orchestrator). Independently verified before merge: `git diff` against base confirmed the pushed branch was exactly the 4-line array addition to the single e2e file - again the Codex UI's file-change panel transiently showed unrelated `pnpm-lock.yaml`/`pnpm-workspace.yaml` changes that were not in the actual pushed diff (same environment quirk as SF-258, now a known pattern - always verify with `git diff origin/<base>...origin/<branch>` rather than trusting the UI panel); safety grep clean; both CI checks green (ready-for-review, not draft, mergeStateStatus CLEAN); local re-verification post-merge: Playwright 2/2 (desktop + mobile Chromium). Verified + merged by orchestrator (Claude), 20 July 2026. |

| SF-260 | Done | UX / Content | International evidence-based discovery scenarios - two new simulation-only scenarios added to `src/data/scenarioLibrary.js`, grounded in a new international-comparative literature review (`docs/public-demo-pack/build-readiness/international-comparative-synthesis.md`): (1) `scenario-respiratory-rate-first` - reviewer must see that respiratory rate is missing/auto-filled rather than counted, and that its subtle trend is the earliest cue (evidence: Hamada 2025 BMJ Open Qual, Japan - RR most-neglected/highest-value vital); (2) `scenario-premet-worried-criterion` - a documented nurse "worried/concern" is a legitimate escalation trigger even with a sub-threshold NEWS2, with a hazard capturing "hierarchy implied: the nurse cannot escalate without a number" (evidence: Sprogis 2021 pre-MET tier; Allen 2017 - hierarchy inhibits escalation). Both follow the exact existing object shape, stay review-support/fictional/non-prescriptive, and carry evidence-citing code comments. | Squash merge commit `6fb62a4` via PR [#74](https://github.com/simionolimpiu-spec/wardsafe/pull/74); `scenarioLibrary.js` (+28) and `scenarioLibrary.test.js` (+87) only. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); correctly narrowed its new collection-wide safety assertion to the two new scenarios after finding existing scenarios legitimately name banned terms in their hazards-to-avoid arrays, and self-diagnosed the two `TrustNetworkView` 60s-timeout non-failures. Independently verified: real pushed diff exactly the 2 scenario files (UI panel's phantom pnpm entries ignored per the known pattern), safety grep clean, no wardLibrary/trustNetwork touched. Initially blocked by an environmental npm-audit advisory gate (see SF-261); after that fix was merged and pulled into this branch, both CI checks passed. Local re-verification post-merge: `scenarioLibrary.test.js` 6/6, `npm audit` 0 vulnerabilities. Verified + merged by orchestrator (Claude), 22 July 2026. |
| SF-261 | Done | Infra / Security | npm dependency-advisory remediation (unblocked the CI merge gate) - three newly-published advisories (fast-uri GHSA-4c8g-83qw-93j6 / GHSA-v2hh-gcrm-f6hx, brace-expansion GHSA-3jxr-9vmj-r5cp, esbuild GHSA-g7r4-m6w7-qqqr), all transitive/mostly under `aws-cdk-lib`, had begun failing the repo's `npm audit --audit-level=moderate` CI step (safeflow-ci.yml) on every PR including SF-260. Fixed via a `package.json` `overrides` block pinning `fast-uri` 3.1.4, `brace-expansion` 5.0.7, `esbuild` 0.28.1, with esbuild's install script approved through the repo's sanctioned allow-scripts flow (recorded as `esbuild@0.28.1` in allowScripts) - the hardened install policy was NOT weakened or bypassed. | Squash merge commit `18e0de0` via PR [#75](https://github.com/simionolimpiu-spec/wardsafe/pull/75); `package.json` (+8 overrides/allowScripts) and `package-lock.json` (dedup) only, no source/test files touched. Built by Codex (Oli lane, driven live via computer-use); correctly diagnosed that `aws-cdk-lib` reference resolution let top-level overrides reach the flagged copies once reinstalled. Independently verified before merge: diff scoped to the 2 dependency files, both CI checks green, and a local `npm audit --audit-level=moderate` returning "found 0 vulnerabilities" on the branch tree. Verified + merged by orchestrator (Claude), 22 July 2026. |

| SF-262 | Done | UX / Content | Alarm-fatigue + speak-up discovery scenarios - two further simulation-only scenarios added to `src/data/scenarioLibrary.js` from the international-comparative synthesis, continuing SF-260: (1) `scenario-alarm-fatigue-triage` - a continuous-monitoring/deterioration-alert stream where most alerts are non-actionable noise and one genuine early cue risks being lost; reviewer must separate actionable from non-actionable and see a tiered/triaged view, escalation staying a human decision (evidence: Pan 2026 J Nurs Manag, 20-country mixed-methods on CM-DAS adoption - alarm fatigue/false-alarm burden is the real barrier); (2) `scenario-graded-assertiveness-speakup` - a junior nurse must escalate a concern up the hierarchy with a structured, editable, graded-assertiveness/SBAR script, speaking-up framed as legitimate regardless of grade and audited non-punitively (evidence: Pozzobon 2025 BMJ Open Qual, Canada communication-openness toolkit; Chua 2023 J Clin Nurs, Singapore nurse-as-middleman). Collection now 14 scenarios; both follow the exact existing object shape, stay review-support/fictional/non-prescriptive, and carry evidence-citing code comments. | Squash merge commit `90f2a05` via PR [#76](https://github.com/simionolimpiu-spec/wardsafe/pull/76); `scenarioLibrary.js` (+28) and `scenarioLibrary.test.js` (+87/-9) only. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); scoped its strict copy-scan around the speak-up scenario's legitimate "diagnosis"-in-hazards wording (same pattern as SF-260), and worked around the local pnpm allow-scripts `ERR_PNPM_IGNORED_BUILDS` pre-check on esbuild@0.28.1 by running the installed Vitest/Playwright binaries directly - without touching dependency files. Independently verified before merge: real pushed diff exactly the 2 scenario files (UI panel's phantom pnpm entries ignored per the known pattern), safety grep clean, no wardLibrary/trustNetwork/dependency files touched, both CI checks green (CLEAN - advisory gate stayed fixed from SF-261). Local re-verification post-merge: `scenarioLibrary.test.js` 8/8, full suite 480/480, e2e 2/2. Verified + merged by orchestrator (Claude), 22 July 2026. |

| SF-263 | Done | UX / Content | Staffing & skill-mix context panel (Scenario Library) - a standing, simulation-only informational panel added to the Discovery Scenario Library reinforcing SafeFlow's core honesty: staffing/skill-mix is the strongest upstream driver of deterioration outcomes, so the tool supplements adequate professional-nurse staffing and is never a substitute for it. New `staffingContextNote` export in `src/data/scenarioLibrary.js` (title + four non-prescriptive points + Aiken 2016 evidence line) rendered as a sibling `<aside className="scenario-staffing-context">` in `ScenarioLibraryView.jsx`, next to the existing hazard-controls aside. Points explicitly state it makes NO staffing-level recommendation and NO clinical claim, and that human review/clinical judgement remain central. Evidence: Aiken et al. 2016, BMJ Quality & Safety (6-country skill-mix study); see international-comparative-synthesis.md. | Squash merge commit `4574498` via PR [#77](https://github.com/simionolimpiu-spec/wardsafe/pull/77); `scenarioLibrary.js` (+11), `ScenarioLibraryView.jsx` (+10/-1), `scenario-and-audit.css` (+31), `scenarioLibrary.test.js` (+28) and `safetyLanguageSurfaces.test.jsx` (+19). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); the new panel text runs through the existing `scanStrictSafetyLanguage` helper and the rendered-surface scan; worked around the local pnpm `ERR_PNPM_IGNORED_BUILDS` esbuild pre-check by running the installed vitest/playwright binaries directly, no dependency files touched. Independently verified before merge: real pushed diff exactly the 5 expected files (phantom pnpm panel entries ignored per the known pattern), safety grep clean, no wardLibrary/trustNetwork/dependency files, both CI checks green (CLEAN). Local re-verification post-merge: scenario + safety-surfaces tests 17/17, full suite 482/482, e2e 2/2. Verified + merged by orchestrator (Claude), 22 July 2026. |

| SF-264 | Done | UX / Domain | "Same score, different system" comparison mode (Scenario Library) - the synthesis Theme-1 teaching idea made real: a contained "Scoring systems comparison (simulation)" panel where the learner picks one of three fictional observation snapshots (early subtle change / moderate deterioration / consciousness change) and sees the SAME observations scored side-by-side under NEWS2 (England convention, RCP 2017 bands, SpO2 scale 1, ACVPU) and a MEWS-style variant (published Subbe-style table, correctly with no SpO2 row) - per-parameter points, totals, each convention's review-threshold line, and a fixed comparison note that local escalation policy always governs and human review is required. New pure domain module `src/domain/scoreComparison.js` (deterministic, no side effects, explicit "teaching renderings of published scoring conventions for FICTIONAL observations, not clinical calculators" comment) + panel in `ScenarioLibraryView.jsx` + styles + safety-surface coverage. Orchestrator independently re-verified every banding function against the published RCP 2017 NEWS2 chart and Subbe 2001 MEWS table - all faithful. | Squash merge commit `aa32485` via PR [#78](https://github.com/simionolimpiu-spec/wardsafe/pull/78); `scoreComparison.js` (+200) + test (+99), `ScenarioLibraryView.jsx` (+89), `scenario-and-audit.css` (+201), `safetyLanguageSurfaces.test.jsx` (+20). Built by Codex (Oli lane, driven live via computer-use); validation 81 files/489 tests, e2e 2/2, build/audit(0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild pre-check with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, safety grep clean (diagnosis/prescribe hits none), both CI checks green (CLEAN). Post-merge: scoreComparison + safety-surfaces tests 15/15. Verified + merged by orchestrator (Claude), 22 July 2026. |

| SF-265 | Done | UX / Content | Family concern / Martha's Rule pathway scenario - completes the escalation-voice trilogy (staff score -> nurse concern SF-260 -> junior-to-senior speak-up SF-262 -> now the patient/family voice): `scenario-family-concern-marthas-rule` in `src/data/scenarioLibrary.js` (collection now 15). A fictional, unnamed relative raises a "not themselves" concern while observations remain unremarkable; the reviewer must see the family-raised concern as a legitimate, visible escalation trigger with the SAME legitimacy as a staff concern, and see whether the family was made aware of the patient/family escalation pathway - directly reflecting the verified evidence that most families are unaware such pathways exist. Hazards include family concern treated as less credible, and pathway-advertised-but-awareness-never-checked. Evidence: NHS England Martha's Rule programme data Sept 2024-Apr 2026 (14,654 calls, 72% via family/carer route, 616 escalations of care; phase 2 rollout to all English acute inpatient sites) and the Frontiers in Health Services 2026 evaluation (93% of surveyed patients/relatives unaware); lineage Call 4 Concern/Ryan's Rule/Condition H - see `evidence-horizon-scan.md`. Sensitivity constraints held: no real patient/family names in scenario copy, fictional unnamed relative, respectful non-sensational tone, review-support framing only. | Squash merge commit `cf59f2a` via PR [#79](https://github.com/simionolimpiu-spec/wardsafe/pull/79); `scenarioLibrary.js` (+14) and `scenarioLibrary.test.js` (+35/-2) only. Built by Codex (Oli lane, driven live via computer-use); self-caught the safety-helper requirement to include "human review" in reviewer-facing copy; ran validation with installed binaries around the pnpm esbuild guard (490/490 unit with --testTimeout=120000, e2e 2/2, build/audit 0 vulns/manifest/synths/migration-plan green), no dependency files touched. Independently verified before merge: diff exactly the 2 scenario files, safety grep clean (incl. no real names - Mills/Merope absent), both CI checks green (CLEAN). Post-merge: scenarioLibrary tests 10/10. Verified + merged by orchestrator (Claude), 23 July 2026. |
| SF-266 | Done | UX / Content | Croskerry named-bias awareness cues (Scenario Library) - a non-interactive "Notice your thinking" reflective aside offering five named cognitive-bias cues (anchoring, premature closure, confirmation bias, availability bias, framing effect) alongside the existing hazard/staffing/score-comparison panels. Explicitly framed as an optional teaching prompt: does not score the scenario, does not change or validate any clinical decision, human review and clinical judgement remain central. New biasAwarenessCues export (title + points[] + evidence) in src/data/scenarioLibrary.js, rendered aside in ScenarioLibraryView.jsx. Evidence: Croskerry on cognitive dispositions to respond and cognitive debiasing (plus Kahneman, Gawande/Pronovost on checklists as debiasing aids); see evidence-horizon-scan.md, section 7. | Squash merge commit c99e866 via PR [#80](https://github.com/simionolimpiu-spec/wardsafe/pull/80); scenarioLibrary.js (+12), ScenarioLibraryView.jsx (+14/-5), scenario-and-audit.css (+33/-2), scenarioLibrary.test.js (+28/-1), safetyLanguageSurfaces.test.jsx (+30/-1). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-diagnosed and worked around the known 180s TrustNetworkView timeout by rerunning with --testTimeout=120000 (492/492 unit), e2e 2/2, build/audit (0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild guard with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, full content matches spec, UI copy correctly non-interactive and explicitly bounded, both CI checks green (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 21/21. Verified + merged by orchestrator (Claude), 23 July 2026. |
| SF-267 | Done | UX / Content | PEARLS-structured debrief panel (Scenario Library) - a non-interactive "Structured debrief (PEARLS)" reflective aside offering the four PEARLS phases as reflective prompts (Reactions, Description, Analysis, Summary) alongside the existing hazard/staffing/score-comparison/bias-awareness panels. Explicitly framed as a facilitated-simulation-debrief teaching prompt: not a data-capture form, scoring mechanism, or clinical tool, nothing entered/stored/transmitted, human review and clinical judgement remain central. New pearlsDebriefPrompts export (title + points[] + evidence) in src/data/scenarioLibrary.js, rendered aside in ScenarioLibraryView.jsx. Evidence: Rudolph et al., Debriefing with Good Judgment; PEARLS framework (Eppich and Cheng); INACSL Healthcare Simulation Standards of Best Practice; see evidence-horizon-scan.md, section 6. | Squash merge commit 465e39f via PR #81 (https://github.com/simionolimpiu-spec/wardsafe/pull/81); scenarioLibrary.js (+11), ScenarioLibraryView.jsx (+13), scenario-and-audit.css (+29), scenarioLibrary.test.js (+29/-1), safetyLanguageSurfaces.test.jsx (+20). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); full unit suite 494/494 passed without needing the timeout fallback this run, e2e 2/2, build/audit (0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild guard with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, full content matches spec, UI copy correctly non-interactive and explicitly bounded (no data capture claim), both CI checks green (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 23/23. Verified and merged by orchestrator (Claude), 23 July 2026. |
| SF-268 | Done | UX / Content | PACE assertiveness-ladder reference panel (Scenario Library) - a non-interactive "Speaking up: the PACE ladder" reference aside offering the four PACE steps (Probe, Alert, Challenge, Emergency) as communication reference points tied to the graded-assertiveness/speak-up scenario, alongside the existing hazard/staffing/score-comparison/bias-awareness/pearls-debrief panels. Explicitly framed as reference material: not a clinical checklist, scoring mechanism, or instruction to act; does not change or validate a clinical decision, human review and clinical judgement remain central. New paceAssertivenessLadder export (title + points[] + evidence) in src/data/scenarioLibrary.js, rendered aside in ScenarioLibraryView.jsx. Evidence: Martin Bromiley and the Clinical Human Factors Group Learning Hub; PACE (Probe, Alert, Challenge, Emergency) graded-assertiveness framework; see evidence-horizon-scan.md, section 2. Bromiley/CHFG cited factually as source of a published, publicly attributed human-factors framework, not a patient identifier. | Squash merge commit cafbf5e via PR #82 (https://github.com/simionolimpiu-spec/wardsafe/pull/82); scenarioLibrary.js (+11), ScenarioLibraryView.jsx (+13), scenario-and-audit.css (+35/-2), scenarioLibrary.test.js (+30/-1), safetyLanguageSurfaces.test.jsx (+20). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-diagnosed and worked around the known 60s TrustNetworkView timeout by rerunning with --testTimeout=120000 (496/496 unit), e2e 2/2, build/audit (0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild guard with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, full content matches spec, wording carefully non-instructional (Emergency step framed as an in-framework immediate-risk cue, not a command to act), both CI checks green (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 25/25. Verified and merged by orchestrator (Claude), 23 July 2026. |
| SF-269 | Done | UX / Content | Safety-II "what went well" reflective strand (Scenario Library) - completes the panel set with a resilience-framed complement to the existing hazard/risk panel: a non-interactive "What went well (Safety-II)" aside with four reflective prompts inviting the reviewer to notice adaptive/resilient practice, not just hazards, alongside the existing hazard/staffing/score-comparison/bias-awareness/pearls-debrief/pace-ladder panels. Explicitly framed as complementing, not replacing, hazard review; not a data-capture form, scoring mechanism, or clinical tool; human review and clinical judgement remain central. New safetyTwoReflectionPrompts export (title + points[] + evidence) in src/data/scenarioLibrary.js, rendered aside in ScenarioLibraryView.jsx. Evidence: Erik Hollnagel on Safety-II and resilient performance; Learning from Excellence movement (Adrian Plunkett); see evidence-horizon-scan.md, section 4. | Squash merge commit 7c07f43 via PR #83 (https://github.com/simionolimpiu-spec/wardsafe/pull/83); scenarioLibrary.js (+11), ScenarioLibraryView.jsx (+13), scenario-and-audit.css (+29), scenarioLibrary.test.js (+37/-1), safetyLanguageSurfaces.test.jsx (+22). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); self-diagnosed and worked around the known TrustNetworkView timeout by rerunning with --testTimeout=120000 (498/498 unit), e2e 2/2, build/audit (0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild guard with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, full content matches spec, boundary copy correctly states it complements rather than replaces hazard review, both CI checks green (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 27/27. Verified and merged by orchestrator (Claude), 23 July 2026. |
| SF-272 | Done | UX / Content | Inclusive-practice / new-to-service context note (Scenario Library) - follow-on from the SF-270 IEN-transition research: a non-interactive "Inclusive practice: new to this ward?" aside making one narrow, evidence-backed point, that SafeFlow explicit written review criteria are equally legible to any reviewer regardless of tenure on the ward, including staff experiencing transition shock, while being explicit the panel is about review-prompt legibility only, not HR, immigration, pastoral-care, or welfare support, and that SafeFlow supplements and never substitutes for proper preceptorship, pastoral support, and local induction. New newToServiceContextNote export (title + points[] + evidence) in src/data/scenarioLibrary.js, rendered aside in ScenarioLibraryView.jsx, deliberately generic (no IEN-specific or nationality-specific claims about any individual). Evidence: Rajpoot et al. 2024 narrative systematic review; NHS England Pastoral Care Quality Award and NHS Employers Accelerated Preceptorship model; see docs/public-demo-pack/build-readiness/ien-transition-findings-note.md. | Squash merge commit dd4d460 via PR #84 (https://github.com/simionolimpiu-spec/wardsafe/pull/84); scenarioLibrary.js (+11), ScenarioLibraryView.jsx (+13), scenario-and-audit.css (+31), scenarioLibrary.test.js (+29), safetyLanguageSurfaces.test.jsx (+21). Built by Codex (Oli lane, driven live via computer-use by the orchestrator); deliberately kept scope narrow per orchestrator instruction (no IEN mode, no individual-identity claims); full unit suite 500/500 (with --testTimeout=120000 rerun for the known TrustNetworkView timeout), e2e 2/2, build/audit (0 vulns)/manifest/synths/migration-plan all green; worked around the pnpm esbuild guard with direct binaries, no dependency files touched. Independently verified before merge: diff exactly the 5 expected files, full content matches spec, correctly avoids HR/welfare/immigration framing, non-substitution language present, both CI checks green (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 29/29. Verified and merged by orchestrator (Claude), 23 July 2026. |
| SF-271 | Done (Part A) | Docs / Adoption | Super-user / train-the-trainer track, Part A - the last horizon-scan candidate. Concept doc first (same pattern as SF-202/SF-207/SF-107/SF-210): a super-user/facilitator role for SafeFlow must be a training/onboarding convenience only, never an in-app permissions tier or authority (SafeFlow has no accounts/RBAC by design - SF-207 was concept-only for the same reason). Delivered Part A: docs/public-demo-pack/facilitator-quick-start-guide.md (session script, recommended first-session scenario picks, common questions) and docs/public-demo-pack/build-readiness/super-user-train-the-trainer-concept.md (role definition, three-part phased scope, safety boundary), plus a new pack-index.md section. Part B (an in-app facilitator toggle) deliberately NOT built this pass - flagged that ScenarioLibraryView.jsx now has eight sibling info panels (hazards, staffing, new-to-service, bias-awareness, pearls-debrief, pace-ladder, safety-II, score-comparison) and a ninth always-on aside would add to a page that is already visually dense; recommended a panel-consolidation pass (toggle/tabs) before any Part B build. Part C (structured facilitator feedback capture) parked as a documented idea only, pending a decision on whether SafeFlow ever collects real usage data. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 9c4975b, no PR (docs-only, no code/tests affected, only touched while Codexs shared working tree was confirmed idle and on the base branch). Files: facilitator-quick-start-guide.md (+32 new), super-user-train-the-trainer-concept.md (+38 new), pack-index.md (+5). No CI-relevant change; no code dispatch this pass. Recorded by orchestrator (Claude), 23 July 2026. |
| SF-273 | Done | UX / Refactor | Panel consolidation pass, Scenario Library - addresses the panel-overload concern flagged in SF-271: the Scenario Library page had grown to eight always-on sibling info panels (hazards, staffing, new-to-service, bias-awareness, pearls-debrief, pace-ladder, safety-II, score-comparison) plus scenario cards, all expanded simultaneously by default. Converted every panel into a native HTML details/summary element sharing name=scenario-library-panels, giving a zero-JS mutually-exclusive accordion (opening one panel closes the previously open one); the hazards panel carries the open attribute so it stays expanded by default, the other seven start collapsed. All existing panel copy, evidence citations, and boundary language preserved verbatim - only the wrapping structure changed. CSS adds a chevron/hover/focus-visible affordance on the new summary element and preserves each panels existing accent colour. | Squash merge commit 9a75acf via PR #85 (https://github.com/simionolimpiu-spec/wardsafe/pull/85); ScenarioLibraryView.jsx (186 insertions/90 deletions combined with css), scenario-and-audit.css (+68/-9 approx). Built by Codex (Oli lane, driven live via computer-use by the orchestrator). Independently verified before merge: diff scoped to exactly these 2 files (no data or test files touched, as expected for a pure rendering refactor); full JSX diff read end to end confirming all eight panels wrap verbatim content in details/summary with the shared accordion name, and that only the hazards panel carries the open attribute; full CSS diff read confirming the chevron/rotate-on-open styling and per-panel colour reassignment with no unrelated changes; both CI checks green on both matrix runs (CLEAN). Post-merge: scenarioLibrary + safety-surfaces tests 29/29 unchanged (details/summary keeps all content in the DOM regardless of open state, so no test changes were needed or made). Verified and merged by orchestrator (Claude), 24 July 2026. |
| SF-274 | Done | Docs / Evidence | HSSIB/Francis/Berwick demo-pack narrative anchors - closes the last unactioned item (5 of 6) on evidence-horizon-scan.mds priority list. Added a Pillar 4 to evidence-base-references.md citing three UK statutory/national investigation reports (HSSIB 2024 recognising-and-responding-to-critically-unwell-patients, Francis 2013 Mid Staffordshire inquiry, Berwick 2013 promise-to-learn review) and a short Why this matters paragraph to master-narrative.md, explicitly non-claiming (SafeFlow does not claim to have solved any finding in these reports, only that they explain why the simulated practice areas exist). Findings note at build-readiness/hssib-francis-berwick-findings-note.md. All three source URLs fetched and confirmed live before citing. Also recovered four prior research deliverables (evidence-base-references.md, evidence-horizon-scan.md, international-comparative-search-protocol.md, international-comparative-synthesis.md) that were written to disk in earlier sessions but never committed to this branch, despite being cited by path from CONTROL.md rows SF-260/262/264/265/266/268/269 - a real gap caught and fixed this pass. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 5bcf788 (docs, 6 files, 734 insertions), no PR (docs-only). Safety-language scan (safetyLanguage.test.js) re-run clean 7/7 after the master-narrative.md edit before committing. Separately, the push triggered a genuine CI failure unrelated to the docs content: a freshly-published npm audit advisory for postcss (GHSA-r28c-9q8g-f849) red-failed Application quality on the live advisory registry (same recurring class of issue as SF-261). Fixed via package.json overrides (postcss to 8.5.23); a second, distinct brace-expansion advisory (GHSA-3jxr-9vmj-r5cp, different range than the SF-261 fix) then surfaced on a nested aws-cdk-lib copy that the existing override was not reaching until node_modules and package-lock.json were deleted and reinstalled from scratch (in-place npm install alone did not propagate the override to that nested copy - new lesson). Fixed via bumping the brace-expansion override to 5.0.8 plus a clean reinstall; npm audit then reported 0 vulnerabilities. Commit 20a8e86. Post-fix full verification: 81/81 files, 500/500 tests, build clean, both CI checks green. Verified and merged by orchestrator (Claude), 24 July 2026. |
| SF-275 | Done | Docs / Safety QA | Closed every outstanding RED gap flagged in master-narrative.md (all except the white-paper-baseline note, which is a deliberate out-of-repo decision). (1) PatientJourneyTwin.jsx presented "Simulation Patient Twin" as a competing product name in the eyebrow directly above the "Patient Journey Twin" h2 - the only RED row in the Discrepancy Table, open since the O7 alignment pass. Every other view uses a descriptive category in that slot (Learning layer, Simulation insight, Fictional record), so the eyebrow now reads "Simulation longitudinal review"; the approved primary name stays on the h2 and the simulation qualifier stays in the boundary note beneath. (2) The automated wording scan gated only 23 of the repos 72 tracked markdown files, which is what left the "broader docs tree still contains older wording" RED open - now extended to every stakeholder-facing doc (62 files), including the whole build-readiness and templates trees. All 49 previously-unscanned files were scanned first and 48 were already clean. (3) The single flag was a scanner false positive: demo-pack README.md lists "prescribing" under "## Out Of Scope", and the boundary-aware negation pattern recognised neither "out of scope" nor the plural "claims" - fixed in safetyLanguage.js rather than by weakening correct copy (also added exclude/excluded). CONTROL.md and docs/superpowers/** deliberately left ungated: both verified clean, but gating a high-churn tracker and historical plan records would fail CI on routine bookkeeping rather than on a real wording regression. (4) Resolved the two long-standing git stashes (pre-merge-safety-stash-2026-07-05, flagged as an open loose end since 5 July, and pre-accessibility-pass): both independently verified superseded - the formers safety-scan extension is now a strict subset of this change, and its Quality Intelligence draft actively conflicts with the merged O7 wording (it would have re-added "patient voice" and "social/discharge coordination" as APPROVED terms against the current banned list); the latter predates SF-124, merged via PR #17. Both preserved as branches pushed to origin under archive/ before the stash entries were cleared, so nothing was deleted. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 005bc3a, no PR (small, self-verified, base branch confirmed before writing). Files: master-narrative.md (Honest Gaps + Discrepancy rows flipped RED to GREEN with the reason recorded), PatientJourneyTwin.jsx (1 line), safetyLanguage.js (negation pattern), safetyLanguage.test.js (+39 gated docs). Verified: targeted run 25/25 (safetyLanguage + PatientJourneyTwin + safetyLanguageSurfaces), then full suite 81/81 files and 500/500 tests, and archive refs confirmed present on origin by SHA before dropping the stashes. Recorded by orchestrator (Claude), 24 July 2026. |
| SF-275b | Done | CI / Safety QA | Split the CI dependency-advisory gate by reachability, after the single npm audit gate broke three times in two days on newly-published advisories against the same transitive package with no code change on our side (postcss GHSA-r28c-9q8g-f849, then brace-expansion GHSA-3jxr-9vmj-r5cp, then brace-expansion GHSA-mh99-v99m-4gvg). Root cause of the third and the reason the earlier override fix only appeared to work: brace-expansion is VENDORED INSIDE the aws-cdk-lib tarball ("inBundle": true in package-lock.json), so npm overrides structurally cannot reach it, and the newest aws-cdk-lib (2.262.1, already installed) still bundles an affected version - there is no upstream fix to take. A permanently-red gate on an unfixable issue trains people to bypass it, which is the worst outcome for a project whose clinical-safety story cites CI, so the gate now splits on whether an advisory can actually reach a user: BLOCKING on npm audit --audit-level=moderate --omit=dev (anything reaching the browser bundle or deployed Lambda; reported 0 vulnerabilities at the time of the split), and NON-BLOCKING (continue-on-error) on the full dev-inclusive audit so the signal is retained and we can bump when upstream ships a fix. Verified before changing anything: aws-cdk-lib is a devDependency only and not a runtime dependency, npm audit --omit=dev is clean, and brace-expansion is absent from the built bundle in dist/. Rationale and the two obligations the split creates (review the non-blocking output every run; the split is by reachability, not convenience, and no allowlist exists for the shipped gate) are documented in a new "Why the advisory gate is split" section of ci-quality-gates.md - which is itself now covered by the safety-language scan via SF-275. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit fd5f4fe, no PR. Files: .github/workflows/safeflow-ci.yml, ci-quality-gates.md. EXPLICITLY APPROVED BY OLI BEFORE IMPLEMENTATION rather than decided unilaterally, because it narrows a safety gate - three options were presented (split by reachability / expiring allowlist via audit-ci / leave red and wait for AWS) and the split was chosen. Verified: safety-language scan 7/7 clean after the ci-quality-gates.md edit, then full SafeFlow CI run green on the base branch. Recorded by orchestrator (Claude), 25 July 2026. |
| SF-277 | Done | Evaluation / Docs | Evaluation session run-ready pack - closes the operational gap that has been the real bottleneck since SF-210 went "Ready to run". The instrument, run plan, facilitator script and capture sheet were all complete, but there was no folder to write results into, no report template, and the remaining step was an open-ended "organise an evaluation". Created docs/evaluation-reports/ with a README stating the boundary (usability and usefulness evidence only, never clinical), the pre-registered thresholds, and participant-anonymity rules; a session-report-template.md keyed to measures M1-M8 that forces every pre-registered threshold to be scored MET or NOT MET with the actual number INCLUDING misses, and includes mandatory "what did not work" and "what this session does not show" sections; and build-readiness/evaluation-recruitment-checklist.md covering the ethics-scope decision (HRA "Is my study research?" decision tool, with an explicit flag that recruiting via a university or intending to publish changes the answer and needs institutional ethics), recruitment (including the coercion check when a manager recruits their own direct reports), scheduling, the on-the-day guest-network failure mode plus offline fallback, and same-day write-up. The framework Q9 safety-concern threshold is restated in both new docs as a STOP CONDITION: a hazard-log entry is required before another session is scheduled. Wired into pack-index.md, the evaluation-readiness-summary.md checklist, and the safety-language scan. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 2583b76, no PR (docs-only, base branch confirmed first). Files: evaluation-reports/README.md (new), evaluation-reports/session-report-template.md (new), evaluation-recruitment-checklist.md (new), evaluation-readiness-summary.md, pack-index.md, safetyLanguage.test.js (+3 gated docs). All three new docs verified against the boundary-aware safety scan (7/7 clean) before committing. SF-210 remains correctly "Ready to run" - the outstanding step is now purely booking participants, and there is a checklist for that. Recorded by orchestrator (Claude), 25 July 2026. |
| SF-276 | Done | UX / Adoption | SF-271 Part B - in-app facilitator notes panel, the piece deliberately held back last session pending the panel-consolidation pass. Now that SF-273 has converted the Scenario Library to a native mutually-exclusive accordion, the facilitator content could be added as one more collapsible panel without worsening page density, which was the exact concern that caused the hold. New facilitatorSessionNote export (title + points[] + evidence) in src/data/scenarioLibrary.js with six condensed facilitation points (pick 3-4 scenarios not all of them; say aloud every session that this is simulation-only and no patient is real; run any baseline task BEFORE showing SafeFlow or the before-and-after comparison is meaningless; ask what felt different from how the ward actually works rather than only what people liked; close by restating this is a discussion aid and not a replacement for local escalation policy, professional judgement, or existing NHS pathways; pointer to the full session script in facilitator-quick-start-guide.md), rendered as a details/summary panel joining the shared scenario-library-panels accordion group, closed by default (hazards retains the open attribute). CRITICAL BOUNDARY, explicitly specified in the dispatch and verified in the diff: the facilitator role carries NO authority inside the tool - the panel states that facilitator notes are a training and onboarding convenience only, conferring no permissions, no accounts, and no authority inside SafeFlow, and that every reviewers structured observation carries equal weight. NO accounts, login, roles, permissions, RBAC, or facilitator-mode state was built; SafeFlow still has no user identity by design, consistent with SF-207 being concept-only for the same reason. This closes SF-271 Parts A and B; Part C (structured facilitator feedback capture) remains parked as a documented idea pending a decision on whether SafeFlow ever collects real usage data. | Squash merge commit 50f70fb via PR [#86](https://github.com/simionolimpiu-spec/wardsafe/pull/86); ScenarioLibraryView.jsx (+16), safetyLanguageSurfaces.test.jsx (+25), scenarioLibrary.js (+13), scenarioLibrary.test.js (+30), scenario-and-audit.css (+28) - 112 insertions, ZERO deletions. Built by Codex (Oli lane, driven live via computer-use by the orchestrator); it self-verified the base branch was clean and aligned with origin before cutting the branch, self-caught and fixed one over-strict test assertion (the guide path legitimately appears twice, in a point and in the evidence citation), and correctly ran the NEW blocking advisory gate (npm audit --audit-level=moderate --omit=dev, 0 vulnerabilities) introduced hours earlier by SF-275b without touching dependency files. Expected GitHub-connector 403 then gh CLI fallback, and opened as a draft as it now tends to. Independently verified before merge: diff scoped to exactly the 5 expected files with insertions only; full content matches spec; grepped the entire diff for useState/localStorage/role/permission/account/login/facilitatorMode/open-attribute and confirmed every hit is boundary copy or a test assertion, with no identity or permission state introduced anywhere; all 4 CI checks green across both matrix runs (CLEAN). Post-merge: scenarioLibrary + safety-surfaces + safetyLanguage tests 38/38. Verified and merged by orchestrator (Claude), 25 July 2026. |
| SF-278 | Done | UI / Branding | NHS-realistic v2 visual pass - implements the SF-202 concept doc, which had sat as a design direction since it was written. Token-level refinement only: no component restructuring, no behaviour change, and zero user-facing copy changed. TWO REAL DEFECTS FIXED, both identified by the orchestrator before dispatch by reading the live code rather than trusting the concept doc. (1) BRANDING: tokens.css set --sf-blue to #005eb8, which is EXACTLY the official NHS Blue hex, and the chart palette in HospitalInsightsView.jsx additionally hard-coded both #005eb8 and #003087 (official NHS Dark Blue). SafeFlow must never imply official NHS branding or endorsement, and using the exact NHS Blue is the strongest possible visual implication of endorsement short of the logo itself - this had been shipping in the live public demo. Replaced with a clearly NHS-adjacent but distinct teal family (--sf-blue #176b75, deep #124c56, ink #0b3640, plus new mid/border/selected/rgb roles; chart brand #176b75, brandDark #0b3640, and the matching rgba fill). Codex independently found the hard-coded copies outside the token, which the dispatch had not enumerated. A repo-wide grep confirms ZERO NHS Blue or NHS Dark Blue literals remain anywhere under src/. (2) TYPOGRAPHY: 99 declarations used font-weight 800 or 900, which the concept explicitly called out as over-use of very heavy weights; now ZERO, with hierarchy rebuilt from size and spacing instead - type scale moved to the concepts intended progression (base 1rem/16px, was 0.92rem), spacing scale genuinely opened up (12/16/20/24/28/32/36/40, was 10/12/14/16/18/20/22/24), new line-height tokens (body 1.55), and weight tokens capped at semibold 600 / bold 700. The now-unused @fontsource/inter/800.css import was dropped from main.jsx. Also broadened the :focus-visible selector to cover summary, [role=button], [role=link] and [tabindex] - a real accessibility gap, since the native details/summary accordions introduced by SF-273 had no visible focus state until now. Print stylesheet hardened for the Ward Quality export; responsive.css gained token-driven mobile containment. | Squash merge commit 46168cb via PR [#87](https://github.com/simionolimpiu-spec/wardsafe/pull/87); 14 files (12 CSS + HospitalInsightsView.jsx palette + main.jsx font import), 351 insertions / 188 deletions. Built by Codex (Oli lane, driven live via computer-use by the orchestrator) over ~30 minutes. It went beyond the automated suites and did rendered desktop/mobile/print browser QA, which caught and fixed two layout regressions the test assertions could not have caught (enlarged summary-card text needed a three-column auto-fit inside the split dashboard; the mobile header/nav needed token-driven responsive containment - mobile navigation now has no horizontal overflow, which the concept explicitly asked for). MEASURED accessibility rather than asserted it: focus ring 3px at 13.00:1, and the tightest changed text/background pair at 4.56:1, both above the WCAG 2.1 AA 4.5:1 floor; all inspected status surfaces retain a text label or icon so nothing relies on colour alone. Correctly diagnosed the known TrustNetwork render-test timing artefact (501/502 with one test exceeding its 60s per-test ceiling by ~8s while the build ran in parallel) as a timing rather than assertion failure, and reran isolated with a 120s ceiling for the authoritative result. Independently verified before merge: diff scoped to the 14 expected files; the only JSX changes are hex values and one font import, with no copy edits anywhere; chart disclaimer byte-for-byte unchanged; font-weight 800/900 count is 0 on the branch; NHS literal grep empty; all 4 CI checks green across both matrix runs (CLEAN). Post-merge on base: NHS literal grep still empty, safetyLanguage + safety-surfaces + HospitalInsights tests 24/24. Verified and merged by orchestrator (Claude), 25 July 2026. |
| SF-279 | Done | Docs / Provenance | Versioned the product white-paper baseline into the repo - the LAST remaining RED in master-narrative.mds Honest Gaps table, which recorded that the baseline existed only in the Aurora strategy pack outside the repo. Every gap in that table is now GREEN. Deliberately versioned as a DATED HISTORICAL BASELINE at docs/product/2026-07-01-product-white-paper-baseline.md rather than imported as a current product description, because importing it as current would have been actively misleading and would have reintroduced exactly the documentation drift the master-narrative control rule exists to prevent: its roadmap listed Phases 1-4 as pending when all four have shipped; its control-board summary listed the Patient Journey Twin, AWS mock layer, evaluation framework and v2 screen concept as in-progress or backlog; its next-actions told the reader to create CONTROL.md and re-confirm a long-resolved PR; and section 5 used "Simulation Patient Twin" as an alternate product name, the exact competing label SF-275 had just removed from the interface. The body is byte-identical to the source (verified programmatically, not by eye), with a header above it stating the document is superseded, pointing to master-narrative.md as the controlled source, and listing what has materially changed. Worth recording that its safety-boundary section held unchanged and is still accurate - the boundary has not drifted in a month of heavy building, only the status claims did. Also added docs/product/README.md explaining the folder and why baselines are NOT gated by the safety scan: a baseline is only worth keeping if unaltered, and gating one would eventually force a choice between failing CI and rewriting the historical record. The README itself IS gated. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit ce8a81f, no PR (docs + scanner, base branch confirmed first). SECOND SCANNER IMPROVEMENT, same class as SF-275: scanning the baseline surfaced two more boundary-aware false positives, both on correct safety-POSITIVE prose - "The incorrect language is ... automated prescribing" (a list of language to avoid) and "Before any movement toward live clinical use, the project would require ... clinical validation" (a list of prerequisites). Both are declarations of what NOT to do. Fixed in safetyLanguage.js by adding "incorrect language", "before any" and "would require" to the negation-context pattern, rather than by editing correct text - this makes the gate more accurate for all 62 gated live documents, where that phrasing is natural. The baseline consequently passes the scan clean and is left ungated by choice, not necessity, which the README states explicitly. Verified: body byte-identical to source, both new docs scan clean, full suite 81/81 files and 502/502 tests. Recorded by orchestrator (Claude), 27 July 2026. |
| SF-280 | Done | Docs / Demo | Fixed a live-demo URL contradiction in public-simulation-preview.md that sent readers to a dead link. The doc contradicted itself: its AWS section asserted "It is the only preview described in this note as active", while a later section correctly stated the GitHub Pages path "is the current active shareable simulation preview". The AWS Amplify frontend sits behind Basic Auth and returns 401 Unauthorized without credentials, so anyone opening the demo pack to find the shareable link hit a dead end before reaching the working one. Found incidentally while verifying SF-278s colour change on the live site - following this docs lead URL produced a 401 rather than the demo, which is how the defect surfaced. Fix: added a "Which URL do I share?" section at the top stating the GitHub Pages URL (https://simionolimpiu-spec.github.io/wardsafe/) plainly, noting it needs no password or token and recording it verified reachable HTTP 200 on 27 July 2026; retitled the AWS section as credentialled infrastructure and explicitly NOT the shareable demo, removing the false precedence claim while retaining the stack details because the backend API genuinely runs there; and marked the GitHub Pages section as the shareable demo. docs/codex-handoff.md carries the same stale Amplify URL but was deliberately left alone - it is a historical handoff note that already opens with a prominent staleness warning covering its AWS/Amplify details and redirects readers to CONTROL.md, the same treatment given to the docs/product/ baselines. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 7347e61, no PR (single docs file, base branch confirmed first). Verified: the contradictory "only preview ... active" claim is gone from the file, the Amplify URL now sits under an explicit not-the-demo heading, and the safety-language scan re-ran clean 7/7 (this doc is gated). Recorded by orchestrator (Claude), 27 July 2026. |

| SF-282/SF-283 | Done | Data / Evidence Corpus | Started the evidence corpus - the queryable database of published research that will inform SafeFlow's runtime suggestions and flags, and later feed the Patient Journey Twin and any LLM layer. This is a new subsystem, not a docs task, so it needed an architecture decision before any code: the key question was what evidence attaches to - a teaching cue TYPE (e.g. "why is a worry criterion taught at all") or a specific fictional patient/scenario instance. Oli chose cue-level, explicitly rejecting patient-level, because patient-level linkage would make SafeFlow's flags look clinically authoritative in a way that risks DCB0129/0160 clinical-safety-case scope and blurs the simulation-only boundary the whole product depends on. That decision, its rejected alternative, and five rules that follow from it are written into docs/public-demo-pack/build-readiness/evidence-corpus-concept.md so it cannot be quietly reversed by someone who does not know it was deliberate. SECOND finding, checked before writing a single record: licensing. Verified copyright status on 5 sampled PubMed records via the copyright-status tool - zero were open access (RCN Publishing, BMJ, Wiley and Cochrane all restrict reuse). The corpus therefore stores only bibliographic metadata (pmid, doi, title, journal, authors, MeSH terms, article types) plus SafeFlow's own original summaries, never publisher abstracts or full text. This is enforced in code, not by convention: scripts/evidence/extract-records.mjs whitelists exactly the safe fields and assertLicenceSafe() throws on anything else, including the literal field names abstract/abstractText/fullText/body/summaryText. Every record also carries a required verification level (metadata-only / abstract-read / full-text-read) so nothing gets treated as read when it has only been found. Oli chose all four first-wave disciplines (core deterioration and nursing, allied health - SLT/physio/OT/dietetics, medical - sepsis/frailty/delirium, and pharmacy/medication safety) rather than narrowing scope. First 20 records extracted and verified against the real PubMed payload shape (journal and authors are nested objects, not strings; no year field exists in this metadata endpoint - year is stored as null by design rather than guessed, pending a separate enrichment step). Includes the landmark Aiken et al. 2002 JAMA staffing/failure-to-rescue study, NEWS2 validation papers, Surviving Sepsis Campaign 2021, delirium assessment tools, dysphagia/aspiration papers and the hospital malnutrition review. Two quality issues found and documented rather than silently ingested: PMID 27885969 is a conference-proceedings collection that matched three separate searches and needs an article-type exclusion filter before scale-up; two records (German PMID 22763869, Spanish PMID 23815057) are non-English and need a language-handling decision. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit c07e14d, no PR (new subsystem foundation, base branch confirmed first, no existing code touched). Verified: extractor tested against a real 20-record PubMed payload with zero abstract leakage (the one string match on "abstract" in the output is the NLM article-type tag "English Abstract", not abstract content - checked by printing the full matching line, not just grepping for the word). CI green on both jobs (SafeFlow CI 2m13s, Deploy simulation preview 50s), run 30356257950 / 30356258160. Remaining work, not yet started: quality filter for conference-proceedings and non-English records, year enrichment, scale-up of the ~54 additional PMIDs already harvested across the 8 searches, the cueTypes taxonomy connecting records to scenarioLibrary.js cue types, SafeFlow-authored summaries, and the cue-to-evidence query layer (SF-284) with tests locking the no-patient-linkage boundary so it cannot regress. Recorded by orchestrator (Claude), 28 July 2026. |

| SF-283b | Done | Data / Evidence Corpus | Closed the three gaps SF-283 left explicitly open: quality filtering, year enrichment, and scale-up beyond the first 20 records. QUALITY FILTER: extract-records.mjs now excludes secondary/non-primary article types (Conference Proceedings, Comment, Editorial, News, Newspaper Article, Retracted/Retraction) and non-English records automatically, writing every exclusion to a companion *-excluded.json with pmid/title/reason rather than dropping it silently. A THIRD problem the automated filter cannot catch surfaced during scale-up and was caught only by reading titles before writing records: PMID 30175361, harvested against the malnutrition search, is a nanotechnology/Raman-spectroscopy paper - a transcription error somewhere in the original horizon-scan list, not a malnutrition source at all. Excluded manually with the reasoning recorded; this is now documented in the concept doc as the argument for keeping a human read of every batch even after the automated filter exists, since type/language checks catch format problems, not topical relevance. YEAR ENRICHMENT: discovered the PubMed metadata payload is inconsistent - some records carry publication_date.year and some do not (the 2002 Aiken JAMA paper had none). Where present, used directly; where absent, a new script (enrich-years.mjs) looks the DOI up in Crossref (api.crossref.org, public and unauthenticated) and reads only the publication year, never guessing - a record without a resolvable DOI keeps year: null. SCALE-UP: processed the remaining ~57 PMIDs harvested across the 8 original searches in 7 further batches, using real full-payload data where the PubMed tool returned it whole and, where a batch exceeded the tool's token limit, reading the safe fields (title/journal/year/articleTypes/language) via targeted grep against the auto-saved raw dump rather than guessing - and where author-list boundaries could not be verified from a flattened grep across the whole batch, leaving authors empty rather than risking a misattributed surname (a real, considered trade-off: honest incompleteness over a wrong-but-plausible-looking fact). New consolidate.mjs script merges every wave file into one deduplicated evidence-corpus.json, re-runnable for future waves. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 7524c5c, no PR (new data files + pipeline script, base branch confirmed first). RESULT: 70 unique, licence-safe records across all four approved disciplines, every one with a real publication year, zero duplicates across waves. Re-verified post-consolidation, not just trusted from the pipeline: all 70 records individually pass assertLicenceSafe(), and a grep for the literal string "abstract" across the full consolidated corpus file returns zero matches (the earlier false-positive risk - the NLM "English Abstract" article-type tag - does not appear because every record carrying that tag was excluded as non-English). Seven records found and excluded, none silently: the one conference-proceedings collection and five non-English records via the automated filter, plus the one topical mismatch by manual read. Notable ingested records include Aiken et al. 2002 JAMA, the Surviving Sepsis Campaign 2021 guidelines, NEWS2 validation studies spanning Singapore/Colombia/India, the FDA-cleared eCARTv5 machine-learning early warning score, 4AT delirium screening validation, ESPEN's 2023/2024 polymorbid nutrition guidelines, and five SBAR/structured-handover studies. evidence-corpus-concept.md updated to state plainly that the corpus is bibliographic-only - no SafeFlow-authored summaries or cueTypes yet exist on any record - so it is not mistaken for more than it is. Verified: safetyLanguage test suite re-run 7/7 green after the doc edit; both CI jobs green (SafeFlow CI 2m10s, Deploy simulation preview 52s), run 30363496870 / 30363497424. Next: SF-284, the cue-type mapping and query layer, with tests locking the patient-level boundary; further scale-up toward the hundreds-scale target is ongoing background work, not blocked on anything. Recorded by orchestrator (Claude), 28 July 2026. |

| SF-284 | Done | Data / Evidence Corpus | Built the cue-to-evidence query layer - the piece SF-282's concept doc named as the thing that would let the corpus actually inform SafeFlow's suggestions, rather than sitting unused. `src/domain/evidenceCorpus.js` is the runtime module: `CUE_TYPES` is a 19-entry taxonomy (7 mapped onto heuristicCueEngine.js's existing signal categories - deteriorating-obs, sepsis-screen, escalation, handover, documentation, discharge, electrolyte-review; 8 onto named scenarioLibrary.js panels and the Martha's Rule scenario - staffing-context, new-to-service, bias-awareness, pearls-debrief, pace-ladder, safety-ii, score-comparison, family-concern; 4 reserved and explicitly marked builtInApp:false for AHP/medical disciplines the corpus already has evidence for but no cue exists yet - dysphagia, delirium, frailty, malnutrition, so the gap between corpus scope and app scope is visible rather than hidden). `getEvidenceForCue(cueType)` is the ONLY query the module exposes, and it takes exactly one string parameter - the structural expression of the cue-level, never-patient-level boundary Oli chose in SF-282. This is locked by a test, not a comment: evidenceCorpus.test.js greps the module's own source for forbidden identifiers (patientId, scenario_id, flagId and other variants) and pins the lookup functions' arity at 1, so a future edit that quietly adds a second, identifier-shaped parameter fails CI rather than merging. All 70 records are now curated: scripts/evidence/build-curation.mjs assigns cueTypes at the level of the topical PubMed search bucket each record was harvested under (honest about its own precision - bucket-level, not hand-tuned per record), with three papers specifically about nurse staffing and failure-to-rescue/sepsis outcomes (Aiken 2002, Ward 2018, Lasater 2020) additionally tagged staffing-context by known PMID. Curation deliberately lives in its own file (curation.json, keyed by pmid) rather than being written into the bibliographic corpus directly, and a new publish step (scripts/evidence/publish.mjs) merges the two into src/data/evidenceCorpus.json - the file the app actually imports - specifically so a future ingestion wave re-running consolidate.mjs can never silently wipe out cue links. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 562bfd6, no PR (new domain module + data pipeline step, base branch confirmed first). A hard line the module enforces on itself: isCitable(record) checks for a non-empty safeflowSummary AND a verification level above metadata-only, and returns false for every one of the 70 records today because none has a summary yet. evidenceCorpus.test.js asserts the citable count is exactly zero right now - not as a placeholder, but so that the day the first safeflowSummary is written this specific assertion starts failing, forcing a conscious update rather than letting citability drift in silently. 14 new tests cover the boundary lock, licence-safety re-verification (zero forbidden fields, zero "abstract" occurrences, corpus size matches the pipeline), curation integrity (every cueTypes value is a recognised CUE_TYPES id), query correctness (unknown cue types return [] rather than throwing, results are newest-first, reserved cue types are valid but may have zero linked records without erroring). Full suite re-run 82/82 files, 516/516 tests green; safetyLanguage scan re-run clean on the updated concept doc. Deliberately NOT done here: no UI component calls getEvidenceForCue() yet - the query layer exists and is tested, but wiring it into a cue panel is separate future work, and doing so before any record is citable would surface unsummarised bibliographic citations next to a live teaching cue, which is exactly the "citation makes a flag look adjudicated" failure mode rule 4 of the SF-282 boundary rules warns against. Both CI jobs green (SafeFlow CI 2m10s, Deploy simulation preview 48s), run 30364700751 / 30364701053. Recorded by orchestrator (Claude), 28 July 2026. |
| SF-285 | Done | Data / Evidence Corpus | Scaled the evidence corpus from 70 to 109 records (wave 3), continuing autonomous work authorised by "ok. go" and "continue" in the previous session, closing gaps the SF-284 report flagged as unstarted: further scale-up toward the hundreds-scale target, and specifically the disciplines that had zero coverage after waves 1-2. PHARMACY/MEDICATION SAFETY (13 records, was 0): UK National Reporting and Learning System high-risk-medication error data, Danish and Australian high-risk-medication registries/Delphi consensus lists, deprescribing in frailty/dementia, discharge medication reconciliation cost-avoidance, a foundation-doctor prescribing-challenges qualitative study. ACUTE KIDNEY INJURY (3 records, was 0): a UK district-general-hospital multidisciplinary "ABCDE" AKI-recognition QI project (judged the best single fit for SafeFlow's ward-teaching style - education plus a simple checklist, measured before/after), the ADQI 16 international consensus on acute kidney disease, and the Taiwan AKI-TASK Force nomenclature consensus. NURSES' WORRY/INTUITION (1 record, was 0): Douw et al. 2015's systematic review of the signs and symptoms that trigger nurses' "worry or concern" before measurable vital-sign change - this was an explicit, named literature gap from an earlier RCN search pass, and is conceptually distinct from the family-concern/Martha's Rule scenario already built (staff intuition vs family voice). Also substantially deepened two thin disciplines: PHYSIO (+17: falls prevention and hospital-associated deconditioning/mobility loss) and OT (+5: functional decline and early supported discharge). Same pipeline as SF-283b throughout: PubMed search, get_article_metadata, extract-records.mjs (licence-safe field whitelist plus the automated article-type/language exclusion filter built in SF-283b), then a manual title screen before anything is written to a wave file - because the automated filter only catches format problems, not topical fit or evidence quality. That manual screen caught 13 further candidates the automated filter would have waved through and excluded them: a pediatric-population duplicate pair (translated/English versions of the same guidance), an ambulatory-oncology-outpatient setting, a vestibular-condition-specific (BPPV) study too narrow to generalise to ward falls guidance, a single case report (excluded on evidence-quality grounds), two niche hematopoietic-stem-cell-transplant population surveys, two records that were simply off-topic for the search bucket they matched (a COVID-19 long-term mental-health study, an out-of-hospital cardiac arrest neurocognitive-testing comparison), a conference-proceedings compilation that PubMed itself mistagged as "Journal Article" so the automated type filter could not catch it (same class of gap as SF-283b's nanotechnology-paper lesson), and one false-positive AKI search match (a Global Burden of Disease paper that matched on "acute" and "kidney" as separate tokens, not actually about AKI). Every one of these 13 is logged with its reason in a new scripts/evidence/wave3-manual-exclusions.json, kept separate from the automated evidence-corpus-excluded.json because none of them would have been caught by that filter alone - the same "never silently drop a record" principle applied to a new failure mode. Five new reserved cue types were added to src/domain/evidenceCorpus.js's CUE_TYPES taxonomy to carry this material: medication-safety, falls-mobility, functional-decline, aki-recognition, nurse-intuition - reserved in the same sense as the four SF-284 added (dysphagia, delirium, frailty, malnutrition): the corpus now has supporting evidence for them, the app has not built a cue panel for any of them yet, and the taxonomy says so plainly rather than hiding the gap. build-curation.mjs gained five new topical buckets mapping PMIDs to these cue types; consolidate.mjs and publish.mjs re-run unchanged (no pipeline code needed to change, only new wave data and new bucket definitions). | Committed directly by orchestrator (Claude) via desktop-commander shell, commit 0664420, no PR (data files plus a small, additive domain-module change - five new frozen entries in an existing array - base branch confirmed clean at 12fa728 first, working tree confirmed on codex/safeflow-prototype throughout per the established working-tree-hazard rule). Verified: 109/109 records have a real publication year (no enrichment needed this wave - every hand-assembled raw record carried publication_date.year directly), zero duplicates on consolidate, a grep for the literal string "abstract" across the full published corpus returns zero matches. evidenceCorpus.test.js's 14 structural boundary tests - the no-patient/scenario/flag-identifier source grep, the arity-1 pin on both lookup functions, the citable-count-must-be-zero assertion, the licence-safety and curation-integrity checks - all pass unmodified, meaning the new records and five new cue types slot into the existing SF-284 boundary without requiring any change to the boundary itself. Full suite re-run 82/82 files, 516/516 tests green. Both CI jobs green (SafeFlow CI 1m56s, Deploy simulation preview 44s), run 30381302952 / 30381302959. Concept doc updated with a wave-3 section and an honest revision to the outstanding-work list: 109 has just crossed the low end of "hundreds", it is not the target size, and the remaining gap is now named specifically (no medical-discipline coverage yet beyond sepsis/frailty/delirium/AKI, and every bucket so far is "a handful of strong records" rather than an exhaustive search - later waves could deepen existing buckets, not only open new ones). Deliberately NOT done here, same as SF-284: no safeflowSummary text written, no verification level raised above metadata-only, no UI wiring to getEvidenceForCue() - the corpus remains bibliographic-only by design until those are done as their own separate, reviewed steps. Recorded by orchestrator (Claude), 28 July 2026. |
| SF-286 (dispatch) | Superseded by SF-289 | Data / Evidence Corpus | Dispatched to Oli's Codex: the first 15 SafeFlow-authored evidence summaries, one per currently-active cue type (all 7 built-in-app `heuristicCueEngine.js` cue types plus 8 named `scenarioLibrary.js`/reserved cue types), to prove the summarisation process end-to-end before scaling it to the rest of the 109-then-127-record corpus. Written as a full dispatch brief (`codex-dispatch-SF-286-evidence-summaries.md`, shared to Oli via `present_files`) rather than a short instruction, because the rules are the whole point and not optional: write original sentences characterising the finding, never a lightly-reworded abstract sentence (the extraction pipeline's abstract-copyright boundary depends on this as much as a licensing rule as a quality one); 2-4 sentences in SafeFlow's existing voice; an honest `verification` level (`abstract-read` vs `full-text-read`, never claimed higher than what was actually read); edit only `curation.json` directly, never the generated corpus files; regenerate via `publish.mjs`; deliberately let `evidenceCorpus.test.js`'s citable-count-is-zero assertion fail and consciously update it to expect 15, not treat the failure as a bug to route around; open a PR rather than committing direct, since this is generated content that needs a human read before it represents SafeFlow's evidence base publicly - unlike the mechanical pipeline commits before it. Investigated first whether Codex could be driven directly via computer-use ("full computer-use access" per earlier memory) - checked the actual installed-application list returned by `request_access` rather than trusting that memory, found no standalone "Codex" entry, and told Oli plainly rather than guessing at a workaround; established pattern (Oli pastes the dispatch brief into his own Codex session) resumed instead. | Dispatched by orchestrator (Claude), no PR yet - awaiting Codex. `git branch -a` checked post-dispatch (during SF-287 work) and no `codex/sf-286-*` remote branch has appeared yet; will review the PR against the 7 rules above once it lands, per the brief's own closing "I'll review before merge" instruction. Recorded by orchestrator (Claude), 28 July 2026. |
| SF-287a | Done | Data / Evidence Corpus | Found and fixed a data-loss bug in `scripts/evidence/build-curation.mjs` before it could destroy Codex's in-flight SF-286 work, rather than waiting for the loss to actually happen. The script's `main()` rebuilt `curation.json` from scratch on every run, unconditionally setting every pmid's `safeflowSummary: null` and `verification: 'metadata-only'` - meaning the moment Codex hand-wrote its first summary into that file, any later re-run of this script (e.g. for a new ingestion wave) would silently wipe it back to blank. This is the same "never silently drop or overwrite data" failure class the project has been careful about everywhere else in the evidence-corpus pipeline (extraction exclusions, consolidation dedup) - just previously unguarded in this one script, because it predates SF-286 ever hand-editing `curation.json` directly. Fixed to be merge-safe: for each pmid, if `curation.json` already has a non-empty `safeflowSummary`, that summary and its `verification` level are preserved; only `cueTypes` (and pmids with no prior curation) are recomputed from `BUCKETS`. Verified with a synthetic before/after test - a throwaway script injected a fake summary, ran the rebuilt `main()`, confirmed the summary survived and the preserved/assigned counts in the log line were correct, then the test artifact was reverted via `git checkout --` and the throwaway script deleted, leaving `curation.json` byte-identical to before the test. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit `3c8881e`, no PR (small, isolated pipeline-script fix, landed proactively ahead of Codex's PR rather than blocking on it - working tree confirmed clean and on `codex/safeflow-prototype` immediately before the commit, per the standing working-tree-hazard rule). Landed before SF-286's PR exists and before SF-287's wave-4 buckets were added, specifically so both of those later touches to `build-curation.mjs`/`curation.json` are protected by it. Recorded by orchestrator (Claude), 28 July 2026. |
| SF-287 | Done | Data / Evidence Corpus | Scaled the evidence corpus from 109 to 127 records (wave 4), closing a different kind of gap from SF-285's: not a reserved/not-yet-built cue type, but three cue types the app already ships and surfaces to a user today (`electrolyte-review`, `discharge`, `documentation` in `heuristicCueEngine.js`) that had zero evidence records behind them - a live-cue gap is a higher-priority miss than a reserved-cue gap, so this was chosen over further-deepening already-covered disciplines. ELECTROLYTE-REVIEW (3 records, was 0): the JBDS/Diabetes UK inpatient DKA-management guideline (explicit hypokalaemia recognition/correction guidance), a systematic review of hyponatraemia diagnostic pathways and correction-failure management, and a perioperative-hyponatraemia/rehabilitation-deterioration cohort study in older hip-fracture patients. DISCHARGE (8 records, was 0): UK, Sudanese and Pakistani audits of discharge-summary/discharge-documentation quality, medication-reconciliation-at-discharge studies (an England CCG audit, a Norwegian hip-fracture-medication-compliance audit), and two LLM-generated-vs-clinician-written discharge-summary comparison studies. DOCUMENTATION (7 records, was 0): missed-nursing-care evidence including a Danish national survey and a Polish BERNCA-R cross-sectional study plus two scoping reviews, and documentation-completeness studies in ICU EMR quality-control and in-hospital resuscitation charting. Same pipeline as SF-285: PubMed search and `get_article_metadata`, then a manual title/abstract screen BEFORE writing anything to a raw batch file this time (rather than after, as SF-285 did) - eight further candidates were fetched and read but never included in a wave file at all (an off-topic heart-failure-registry study using hyponatraemia only as a covariate, a paediatric case, a single oncology-drug case report, an abbreviation-comprehension QI project too tangential to discharge safety, a paediatric I-PASS handover study, a surgical-safety-checklist QI project judged different cue territory from nursing documentation, a prehospital-paramedic safety study in the wrong care setting, a health-IT/EHR-barriers qualitative study too tangential to documentation practice) - logged with reasons in a new `scripts/evidence/wave4-manual-exclusions.json` rather than silently omitted, matching SF-285's established discipline of never dropping a screening decision without a recorded reason. SECOND FINDING, not from this wave's own data: while consolidating, the record count came back as 140 instead of the expected 127 (109 + 18). Traced to a real bug in `scripts/evidence/consolidate.mjs`'s file-discovery filter - it excluded files ending exactly `-excluded.json` but SF-285's own `wave3-manual-exclusions.json` doesn't end in that exact suffix, so it was being silently loaded as 13 more "records" (which are actually pmid/title/reason triples, not licence-safe bibliographic records). This had been true since SF-285 but never surfaced because SF-285 never re-ran `consolidate.mjs` after adding that file - SF-287 was the first wave to expose it. Fixed by excluding any filename containing `exclu` rather than one exact suffix (covers `-excluded.json` and `-manual-exclusions.json` alike, and any future naming variant), re-verified back to exactly 127, and re-confirmed stable after adding this wave's own `wave4-manual-exclusions.json` (same naming class, now correctly excluded by the fix). | Committed directly by orchestrator (Claude) via desktop-commander shell, no PR (data files plus a two-line pipeline-bug fix in `consolidate.mjs` and three additive bucket entries in `build-curation.mjs` - base branch confirmed clean on `codex/safeflow-prototype` before starting and re-checked before committing, per the working-tree-hazard rule; no SF-286 branch had appeared in `git branch -a` at time of work). Verified: 127/127 records have a real publication year (all fetched with `publication_date.year` present, no enrichment step needed this wave), 0 duplicates on consolidate, `evidenceCorpus.test.js`'s 14 structural boundary tests pass unmodified against the new data and the corrected count, same as SF-285. Full suite re-run: 515/516 passed with one `TrustNetworkView.test.jsx` failure on its internal 60s timeout - re-ran that file alone with a 180s timeout and it passed in ~84s, confirming this is the known pre-existing slow-suite flakiness already documented in project memory, not a regression from this work. Concept doc (`evidence-corpus-concept.md`) updated with a wave-4 section, the consolidate.mjs bug writeup, and a revised outstanding-work list. Deliberately NOT done here, same as SF-285: no `safeflowSummary` text written by this wave, no verification level raised, no UI wiring to `getEvidenceForCue()` - that remains SF-286's and later work's job. Recorded by orchestrator (Claude), 28 July 2026. |
| SF-288 | Done | Data / Evidence Corpus | User-directed wave 5, scaled the corpus from 127 to 174 records, starting from a ward-level question rather than a cue-type gap: "find all wards that NN, Paget and QEH have... find the 10 most common causes why patients end up in secondary care in those wards... build the database on them." WARD DEDUP: extracted the real ward lists already built for these three trusts in `src/data/trustNetwork/trusts.js` (JPUH 33 wards, NNUH 21, QEH 17) and grouped them by clinical function rather than by literal name, since names are trust-specific (e.g. JPUH's "Ward 12 - Older People's Medicine" and QEH's "Windsor Ward - Frailty Unit" are the same ward type under different names) - arriving at 11 corresponding adult ward-type groups (General/Acute Medicine, Frailty, Respiratory, Cardiology, Surgical/Trauma & Ortho, ED/Assessment, Critical Care, Gastroenterology, Stroke, Renal, Oncology/Haematology), deliberately excluding maternity, paediatrics, neonatal and day-procedure units as out of SafeFlow's declared adult-ward scope. CAUSE LIST: grounded in real NHS/Nuffield Trust admission epidemiology (ambulatory- and urgent-care-sensitive conditions, and the documented fact that emergency admission rates are highest for falls, non-specific chest pain and non-specific abdominal pain) rather than assumption, verified via `WebSearch` per the project's search-first policy for statistical claims - landed on COPD exacerbation, pneumonia/LRTI, heart failure decompensation, UTI/urosepsis, falls and fragility fracture, stroke/TIA, chest pain/ACS, abdominal pain, GI bleed and cellulitis/SSTI, deliberately chosen to avoid overlap with cue types the corpus already covers deeply (sepsis, frailty, AKI, delirium, electrolytes, medication safety). All 10 hit the user's 2-10-articles-per-cause target on the first PubMed pass (3-7 each, 47 total), so the "expand to 15 where needed" instruction's fallback pool (diabetic emergency, alcohol-related admission, seizure, anaemia, hypertension) was identified but not needed and is not built. SCREENING DISCIPLINE: several initial searches were weak or off-topic and were deliberately re-run rather than accepted as-is - pneumonia's first CAP-guideline batch surfaced mostly tangential antibiotic-stewardship/AMR-policy papers (only 1 of 5 usable) and was supplemented with a CURB-65-specific search; UTI's "urosepsis"-keyword batch returned post-surgical ureteroscopy-complication papers rather than ward-based UTI-recognition papers (2 of 5 usable) and was supplemented with an "asymptomatic bacteriuria"/diagnostic-stewardship search that produced 5 strong candidates. Three PubMed hits were excluded for being non-English (`language` field `spa`/`spa`/`dut`) and simply not selected as final candidates - the existing `extract-records.mjs` automated language filter would have caught them anyway had they been routed through it. Falls/fragility-fracture's UK-specific BOAST-audit candidate (a 7-hospital regional trauma-and-orthopaedics audit) was judged an excellent fit given the direct JPUH/QEH/NNUH relevance. PIPELINE VARIATION FROM PRIOR WAVES: authored the 10 new `wave5-*.json` files directly in the already-extracted, licence-safe shape (matching `extract-records.mjs`'s field whitelist exactly: pmid/doi/title/journal/journalAbbrev/year/authors/meshTerms/articleTypes/language) rather than saving raw PubMed payloads and running them through `extract-records.mjs`, since the metadata had already been fetched and manually screened in-conversation - this is functionally equivalent but additionally guarantees no abstract text ever touches disk, since it was never written to a raw file in the first place. One placeholder record (fabricated journal/author fields for a PMID recalled only from a pre-compaction summary) was caught before committing by re-fetching the real metadata and correcting it - a reminder to always re-verify recalled facts against a fresh tool call rather than trusting summarised memory of prior tool output. | Committed directly by orchestrator (Claude) via desktop-commander shell, commit `1908d7d`, no PR (10 new data files plus 10 additive `build-curation.mjs` bucket entries and 10 additive frozen `CUE_TYPES` entries in `evidenceCorpus.js` - base branch confirmed clean on `codex/safeflow-prototype` both before writing files and immediately before committing, per the working-tree-hazard rule). New reserved cue types (none built in the app yet, same `builtInApp:false` pattern as SF-285/SF-287's reserved entries): `copd-exacerbation`, `pneumonia-lrti`, `heart-failure-decompensation`, `uti-urosepsis`, `falls-fragility-fracture`, `stroke-tia`, `chest-pain-acs`, `abdominal-pain`, `gi-bleed`, `cellulitis-ssti`. Verified: 174/174 records have a real publication year, 0 duplicates on consolidate (127 + 47 = 174 exactly), all 47 new pmids unique against each other and the existing corpus, `evidenceCorpus.test.js`'s 14 structural boundary tests pass unmodified including the "every cueTypes entry is a recognised CUE_TYPES id" check against the 10 new ids. Full suite re-run clean: 82/82 files, 516/516 tests. Deliberately NOT done here, same as every prior wave: no `safeflowSummary` text written, no verification level raised above `metadata-only`, no UI wiring to `getEvidenceForCue()` for the new cue types - they join the existing reserved-cue backlog. Recorded by orchestrator (Claude), 30 July 2026. |
| SF-289 | Done | Data / Evidence Corpus | Self-authored the first wave of `safeflowSummary` text, superseding the SF-286 Codex dispatch (which had sat "In progress" since 28 July with no `codex/sf-286-*` branch ever appearing). Oli chose this path explicitly (option "2": Claude writes the summaries directly) rather than continuing to wait on Codex. FIRST, checked real per-cue-type record counts against the 15 cue types SF-286's brief assumed were all populated, rather than trusting that assumption: only 9 of the 15 built-in-app cue types actually have any linked evidence record (`deteriorating-obs` 22, `score-comparison` 11, `escalation` 11, `sepsis-screen` 9, `discharge` 8, `documentation` 7, `handover` 7, `electrolyte-review` 3, `staffing-context` 3). The other 6 (`new-to-service`, `bias-awareness`, `pearls-debrief`, `pace-ladder`, `safety-ii`, `family-concern`) have zero records - no ingestion wave has ever searched for literature supporting those scenario-library pedagogical frameworks - so this wave writes summaries only for the 9 populated types rather than padding or fabricating coverage for the other 6, and says so plainly in the updated test comment rather than leaving the gap implicit. SELECTION: picked one pmid per populated cue type, preferring records that already carried multiple cueTypes so a single summary could honestly serve more than one lens at once - `12387650` (Aiken 2002 JAMA) covers `escalation`+`deteriorating-obs`+`staffing-context` together (failure-to-rescue is an escalation-and-deterioration story, and staffing ratio is the causal variable); `33309843` (Lasater 2020) covers `sepsis-screen`+`staffing-context`; `41142216` (large Singapore NEWS2 validation) covers `deteriorating-obs`+`score-comparison`, chosen over several smaller single-country NEWS2 validation candidates specifically because its multi-outcome, multi-time-window analysis is itself a within-score comparison, not just a bigger sample. The remaining 5 cue types each got one dedicated pmid: `35776738` (sepsis-screen, nurse-practice systematic review), `42338059` (handover, 2026 ISBAR/SBAR systematic review), `39026330` (documentation, ICU EMR quality-control trial), `41446475` (discharge, UK weekend-continuity-checklist QI project), `35224769` (electrolyte-review, JBDS DKA guideline). VERIFICATION: re-fetched all 8 pmids' abstracts fresh via `get_article_metadata` in this session rather than trusting an earlier spawned agent's large, only-partially-readable output file - consistent with the wave-5 fabrication near-miss's lesson to never rely on recalled/relayed tool output for a fact that can be re-verified directly. Every summary is original SafeFlow-voice prose (2-4 sentences), states real reported numbers, and is honest about limitations where the source itself flagged them (e.g. NEWS2's only-moderate discrimination for ICU admission/code-blue despite strong mortality discrimination; the SBAR review's "moderate certainty" language). SECOND FINDING, caught by consciously updating rather than deleting the existing "no record is citable" test: with the first 8 summaries written, the licence-safety test asserting the published corpus contains no literal occurrence of "abstract" anywhere started failing - not because any abstract text leaked, but because `verification: "abstract-read"` (a legitimate, schema-defined enum value documented since SF-282) contains the substring. Fixed the test to strip only the known-safe `"abstract-read"` label before scanning, so the real safety property (no pasted abstract text in any field) is still enforced for everything else. | PR #88 opened from branch `sf-289-evidence-summaries` against `codex/safeflow-prototype`, per SF-286's brief requirement that self-authored evidence content get a human read before merge (unlike the mechanical pipeline waves, which commit direct). Edited only `scripts/evidence/curation.json` (8 records: `verification` and `safeflowSummary` fields only, `cueTypes` untouched) then regenerated `src/data/evidenceCorpus.json` via `publish.mjs` (174 records, 174 with curation, unchanged count). Updated `evidenceCorpus.test.js`'s citable-count assertion from a hardcoded `0` to `8`, with a comment explaining which 9 cue types are covered and why the other 6 are not, and fixed the abstract-substring licence-safety test as described above. While the PR sat open, SF-290 (wave 6) committed directly to `codex/safeflow-prototype` and touched the same four files, so the PR entered `CONFLICTING`/`DIRTY` merge state. Resolved by merging `codex/safeflow-prototype` into `sf-289-evidence-summaries` (commit `e76ff97`): `curation.json`/`evidence-corpus.json`/`src/data/evidenceCorpus.json` auto-merged cleanly on disjoint pmids (git's line-based merge handled it without manual intervention), verified programmatically post-merge that all 194 records, all 8 SF-289 summaries, and all 20 SF-290 wave-6 records survived intact; `CONTROL.md` needed manual resolution to keep both the SF-289 and SF-290 rows in order; `evidenceCorpus.test.js`'s citable-count comment (stale after SF-290, since it said the 6 scenario-library cue types "have no linked evidence records yet") was updated to reflect that they now have records but no summaries, count unchanged at 8. Oli reviewed and approved with "reviewed, all good." Verified post-merge: `evidenceCorpus.test.js` 14/14 passing; full `src/`+`server/` suite 65/65 files, 383/383 tests passing. Squash-merged via `gh pr merge 88 --squash --delete-branch`, commit `f5f18c0` on `codex/safeflow-prototype`. Both CI jobs re-confirmed green on the merge commit (SafeFlow CI 2m7s, Deploy simulation preview 44s), run 30552437359 / 30552437056. Recorded by orchestrator (Claude), 30 July 2026. |
| SF-290 | Done | Data / Evidence Corpus | Wave 6: closed the exact 6-cue-type gap SF-289 found and documented (no ingestion wave had ever searched literature for `new-to-service`, `bias-awareness`, `pearls-debrief`, `pace-ladder`, `safety-ii`, `family-concern` - the six `scenarioLibrary.js`-sourced cue types with zero linked evidence records). Unlike every prior wave, this one did not start from a blank search: `scenarioLibrary.js`'s existing `newToServiceContextNote`/`biasAwarenessCues`/`pearlsDebriefPrompts`/`paceAssertivenessLadder`/`safetyTwoReflectionPrompts` panels each already carry an `evidence:` string naming specific real sources from earlier horizon-scan work (SF-266 through SF-272) - so this wave verified and formally linked those exact named sources via PubMed, rather than searching cold and risking a mismatch with what the app already tells the reviewer. NEW-TO-SERVICE (3): Rajpoot et al. 2024 (the exact paper already named in-app), plus a rural-Alberta IEN wraparound-support study and a bridging-education-programs systematic review found alongside it. BIAS-AWARENESS (3): all three of Croskerry's own foundational papers on cognitive dispositions to respond and debiasing (Acad Emerg Med 2002, Acad Med 2003, NEJM 2013 "mindless to mindful practice") - the in-app cue names Croskerry generically, so all three core papers were linked rather than picking one arbitrarily. PEARLS-DEBRIEF (3): Eppich & Cheng's original 2015 PEARLS framework paper (the exact source named in-app), the 2019 PEARLS for Systems Integration extension, and a 2024 PEARLS-for-equity extension - showing the framework's real development trajectory, not just its origin. PACE-LADDER (3): the PACE framework itself (Probe/Alert/Challenge/Emergency) is Clinical Human Factors Group training material, not a peer-reviewed publication, so no direct PubMed match exists for the named source - this wave instead linked the closest verifiable academic literature on the same underlying concept (graded assertiveness for speaking up): a nursing-student graded-assertiveness intervention study, a systematic review of factors influencing speaking-up behaviour in hospitals, and a psychological-safety review, with this substitution stated plainly rather than silently treated as equivalent. SAFETY-II (3): Braithwaite/Wears/Hollnagel's "Resilient health care: turning patient safety on its head" (the exact Hollnagel source named in-app), Plunkett's own Learning from Excellence quality-improvement study (the exact Plunkett source named in-app), and a 2025 systematic review of Safety-II learning tools in healthcare practice. FAMILY-CONCERN (5, Martha's Rule scenario): since Martha's Rule itself is a 2024 NHS policy mechanism rather than a research paper, this wave linked its closest real academic precedents and evaluations instead - the UK "Call for Concern" model (Royal Berkshire), the US Condition Help original study, a paediatric family-activated-rapid-response systematic review, and two Queensland Ryan's Rule evaluations (the precedent programme Martha's Rule was modelled on) - again, a stated substitution, not a silent one. All 20 pmids individually re-verified via `get_article_metadata` in this session (not recalled), cross-checked against the full 174-record corpus for duplicates before writing any wave file (0 found). Same pipeline variation as SF-288: wave files authored directly in `extract-records.mjs`'s extracted, licence-safe shape rather than routed through raw-payload extraction, since metadata was already fetched and manually screened in-conversation. No new `CUE_TYPES` entries needed - all 6 were already `builtInApp: true` (added in SF-284), this wave only supplies their first linked records. | Committed directly by orchestrator (Claude) via desktop-commander shell, no PR (data files plus six additive `build-curation.mjs` bucket entries, same precedent as SF-285/287/288 for bibliographic-record-only waves - `safeflowSummary` text is the part that requires a PR, per SF-286's brief, and none is written here). Base branch confirmed clean on `codex/safeflow-prototype` before starting (switched back from the `sf-289-evidence-summaries` PR branch first, per the working-tree-hazard rule) and re-checked before committing. Verified: 194/194 records have a real publication year (174+20 exact, 0 duplicates on consolidate), `evidenceCorpus.test.js`'s 14 structural boundary tests pass unmodified against the new data. Full `src/`+`server/` suite (same `infra/`/`database/` CDK-bundling exclusion as SF-289, for the same reason): 65/65 files, 383/383 tests passing. Recorded by orchestrator (Claude), 30 July 2026. |
| SF-292 | Done | Infra / Security | AWS/LLM/ML loose-ends audit (requested by Oli) plus a real new npm dependency advisory found during it. Audit findings: (1) AWS - `SAFEFLOW_DEPLOYMENT_APPROVED` gate confirmed fail-closed (`infra/aws/deploymentApproval.js`) and `environmentProfiles.js` unchanged; there IS a real, currently-live AWS preview stack (`safeflow-simulation-foundation`, Amplify app `d3etfd425b4rlk` + Lambda Function URL in eu-west-2), correctly gated behind Amplify Basic Auth + `X-SafeFlow-Preview-Token` and documented in `public-simulation-preview.md` — this is separate from the shareable GitHub Pages static demo and was not previously flagged as "live" in recent session memory. That stack's `/api/simulation/signals` and `/api/simulation/risk-suggestions` routes still serve placeholder providers, not the real ML model — a gap the doc already self-flags under "Follow-up Integration Note", not a new finding. (2) ML - `simulation-risk-ml-v0-2-sim` unchanged, `MODEL_CARD.md` accurate and current. (3) LLM - a real, working, safety-constrained OpenAI integration already exists (`server/draft/openAiDraftProvider.js`, added by commit `e3ca66e` on 17 June 2026): server-side only, `OPENAI_API_KEY`-gated with a deterministic non-LLM fallback when absent (`server/providerFactory.js`), strict JSON-schema structured output, a fictional-only input contract, and a post-hoc regex safety check rejecting prescribing/diagnostic wording before any draft reaches the reviewer. It is documented in `docs/public-demo-pack/build-readiness/technology-stack.md` and `README.md` but was never given its own SF-ID/CONTROL.md row (the SF-103/106/108 numbering gap where it chronologically belongs) - a real tracking gap, now noted here; no code change proposed since the feature and its safeguards are already sound. (4) New finding independent of the above: `npm audit` surfaced a high-severity brace-expansion advisory (`GHSA-rgw5-rvv9-x895`, "bypassing the CVE-2026-14257 mitigation" - the very mitigation SF-261 shipped) affecting the copy bundled inside `aws-cdk-lib`'s tarball. Confirmed via `npm explain` that this copy is `inBundle: true`, so npm `overrides` cannot reach it — SF-261's original `"brace-expansion": "5.0.8"` override was therefore already silently ineffective against this specific nested copy, not just the newer advisory. This is exactly the scenario SF-275b's non-blocking build-time-only advisory gate in `safeflow-ci.yml` was designed for, so CI was never actually blocked. Bumped `aws-cdk-lib` devDependency `^2.260.0` -> `^2.263.0`, which upgrades the bundled copy from 5.0.7 to 5.0.8 (resolves the older of the two advisories; no newer aws-cdk-lib release exists yet that bundles >=5.0.9, so the newer advisory remains upstream-blocked exactly per SF-275b's documented policy). Left the `overrides` entry at `5.0.9` (forward-looking, takes effect automatically if aws-cdk-lib ever stops bundling its own copy). (5) Also confirmed the CONTROL.md "Current status" header table (top of file, dated 5 July 2026) is stale against the row content below it (now through 30 July) — cosmetic, flagged for a future pass, not fixed here to keep this diff scoped. | `package.json`+`package-lock.json` only (aws-cdk-lib devDependency bump; `brace-expansion`/`fast-uri`/`esbuild`/`postcss` override block otherwise unchanged). Built by orchestrator (Claude) directly via desktop-commander shell on branch `sf-292-brace-expansion-fix` off `codex/safeflow-prototype` at `07bca23` (base still carries the unresolved SF-289/290 merge-conflict markers pending PR #89 - deliberately left untouched here to keep this diff scoped to the dependency fix; PR #89 will resolve them on merge). Verified: `npm audit --audit-level=moderate` before/after shows the vulnerability count dropping from 2 advisories to 1 (both under the same `brace-expansion` entry); `npm ls brace-expansion --all` confirms the bundled copy is now 5.0.8; full suite 81/82 files, 514/516 tests passing on first run (the 2 failures are the same well-documented pre-existing `TrustNetworkView.test.jsx` 60s-timeout flake from SF-287/289/258/259 - re-ran that file alone with `--testTimeout=180000` and got 3/3 passing); `infra`-scoped suite (CDK synth/bundling) 13/13 files, 90/90 tests passing against the new aws-cdk-lib version. Recorded by orchestrator (Claude), 4 August 2026. |
| SF-291 | Done | Data / Evidence Corpus | Second wave of self-authored `safeflowSummary` text, closing the exact gap SF-289 left open and SF-290 supplied records for but not summaries: `new-to-service`, `bias-awareness`, `pearls-debrief`, `pace-ladder`, `safety-ii`, `family-concern`. All 15 built-in-app cue types are now citable (was 9/15 after SF-289). SELECTION: one pmid per cue type, preferring the exact source `scenarioLibrary.js` already names in-app where SF-290 identified one - `38832236` (Rajpoot et al. 2024, new-to-service, the exact named source), `25710312` (Eppich & Cheng 2015, pearls-debrief, the exact named source), `26294709` (Braithwaite/Wears/Hollnagel 2015, safety-ii, the exact named source). Where SF-290 found no single named source, picked the strongest candidate from its shortlist: `12915363` (Croskerry 2003, bias-awareness - the foundational paper on cognitive debiasing strategies, since the in-app cue names Croskerry generically rather than one specific paper); `31698289` (Hanson et al. 2019, pace-ladder - the graded-assertiveness intervention study, since PACE itself is Clinical Human Factors Group training material with no PubMed match, a stated substitution carried forward from SF-290); `34888450` (Bucknall et al. 2021, family-concern - the UK "Call for Concern" model, chosen over the other 4 SF-290 candidates because it is the direct UK/NHS-context precedent Martha's Rule draws on, not just a comparable overseas scheme). VERIFICATION: all 6 abstracts re-fetched fresh via `get_article_metadata` in this session, consistent with the standing discipline of never writing a summary from a recalled or relayed abstract. Every summary is original SafeFlow-voice prose (2-4 sentences), honest about the two stated substitutions (pace-ladder, family-concern) rather than presenting them as if they were the named source. SECOND FINDING, unrelated to the summaries themselves: `CONTROL.md` still contained literal `=======` and `>>>>>>> origin/codex/safeflow-prototype` merge-conflict marker text between the SF-289 and SF-290 rows and at end-of-file - leftover from the SF-289/SF-290 conflict resolution (commit `e76ff97`), where only the `<<<<<<< HEAD` marker had been removed and the other two lines were missed. Caught by directly reading the file rather than trusting a prior tool echo; fixed as part of this commit, verified no marker text remains anywhere in the file afterward. | Edited only `scripts/evidence/curation.json` (6 records: `verification` and `safeflowSummary` fields only, `cueTypes` untouched) then regenerated `src/data/evidenceCorpus.json` via `publish.mjs` (194 records, 194 with curation, unchanged count). Updated `evidenceCorpus.test.js`'s citable-count assertion from `8` to `14`, with a comment explaining the SF-291 wave and noting the 19 reserved (`builtInApp:false`) cue types still have none - future work gated on those disciplines getting cue panels built in the app first. Verified: `evidenceCorpus.test.js` 14/14 passing. Full `src/`+`server/` suite 65/65 files, 383/383 tests passing (one `TrustNetworkView.test.jsx` timeout on the full-suite run, re-confirmed as the same known pre-existing flake documented in the SF-287/SF-289 reports by re-running in isolation with an extended timeout: 3/3 passing). Committed directly by orchestrator (Claude) via desktop-commander shell, no PR required for the marker-fix half (isolated bug fix, not evidence content), but since this commit is bundled with self-authored `safeflowSummary` text it follows the SF-286/SF-289 precedent and goes through a PR - branch `sf-291-evidence-summaries-wave2` opened against `codex/safeflow-prototype`. Working tree confirmed clean and on `codex/safeflow-prototype` before branching, per the working-tree-hazard rule. Recorded by orchestrator (Claude), 30 July 2026. |
