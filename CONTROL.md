# SafeFlow Control Board

**Status date:** 5 July 2026
**Project boundary:** Simulation, education and stakeholder demonstration only. No real patient data. No live NHS deployment. No automated prescribing or escalation.

## Current status

| Field | Current position |
|---|---|
| Overall status | Strong external-facing simulation preview package; live repo status re-confirmed 5 July 2026. |
| Current product boundary | Simulation-only education/demo prototype; human-review support language only. |
| Current branch | codex/safeflow-prototype |
| Open PR | None open as of 5 July 2026 (post-merge check). |
| Closed PR | #7 "Add minimal role-aware GUI foundation", branch feature/minimal-role-aware-gui. State: CLOSED as superseded on 1 July 2026. |
| Merged PR | #29 "test(safety): add regression scans for demo surfaces" (squash merge `c0bbb8a`), #30 "docs: align public demo pack master narrative" (squash merge `29c1974`), and #31 "feat: add ward quality and safety review export" (squash merge `8e1dcbb`, SF-217), all merged 5 July 2026. Post-merge verification: `npm test` 70 files/400 tests passed, `npm run build` passed. |
| Merged PR | #13 "Content views refresh", branch ui/content-views-refresh. Rebased onto latest base, `npm test` (56 files, 338 tests) and `npm run build` passed, safety-wording scan clean, merged 1 July 2026. |
| Deployment guard | SAFEFLOW_DEPLOYMENT_APPROVED now fails closed unless the exact string `true` is present; unset or other values are logged as not approved, and local dev-server providers also refuse placeholder fallback outside allowed preview environments. |
| Primary unfinished control task | Keep this file updated after every commit, doc change, or PR event; keep the app-copy naming cleanup queued as a Mia task. |

## Completed / Done

| ID | Status | Area | Work item | Evidence |
|---|---|---|---|---|
| SF-001 | Done | Safety boundary | Simulation-only boundary established: no real patient data, no live NHS systems, no prescribing/escalation claims. | Project rule / control board |
| SF-002 | Done | Simulation signals | Pure cue engine, workspace signal snapshot storage, app wiring, and patient-panel review cues implemented. | Commit 1e888ae0632898fee488e12def8a1c1b5e9ae96f |
| SF-003 | Done | Patient panel | Simulation-only Review cues section added to patient panel. | Commit 1e888ae0632898fee488e12def8a1c1b5e9ae96f |
| SF-004 | Done | Presentation Mode | Header toggle, presentation banner, simplified flow strip, and projector-friendly styling added. | Commit f55c51b |
| SF-005 | Done | Testing | Presentation Mode enable/disable tests added and focused tests passed. | npm test reported passed |
| SF-006 | Done | Build | Project build validated after Presentation Mode work. | npm run build reported passed |
| SF-008 | Done | Infra tests | infra/aws/runSynth.js compatibility fix applied so Vitest can import reliably. | Commit f55c51b |
| SF-010 | Done | Roadmap | delivery-roadmap.md updated with completed demo features and backlog items. | Commit 10aceac |
| SF-011 | Done | Exportable report | Exportable Simulation Review Report added to demo/readiness status. | Commits 0a20cd5, 105c892, 3620953 |
| SF-102 | Done | Product concept | Patient Journey Twin v2 timeline view shipped with the minimal app/nav wiring and scenario styling. | Squash merge commit `6d98cd9` via PR [#20](https://github.com/simionolimpiu-spec/wardsafe/pull/20). |
| SF-013 | Done | Clinical safety / IG | clinical-safety-ig-readiness.md created covering simulation boundary, clinical safety, IG/data protection, human review, RBAC/audit logging, future NHS/AWS readiness, risks, open questions, and simulation-to-live gates. | Commit d00b662 |
| SF-014 | Done | Stakeholder pack | stakeholder-demo-pack.md created as a concise NHS-facing demo pack. | Commit b74ab9f |
| SF-016 | Done | Control | CONTROL.md created at repo root as the permanent control tracker. | This commit |
| SF-117 | Done | Governance | Deployment guard now fails closed and logs when SAFEFLOW_DEPLOYMENT_APPROVED is unset or not exactly `true`. | Added shared deployment approval reader plus tests; deploy approval is explicit only for the exact string `true`. |
| SF-118 | Done | UI foundation | Design token layer added and `src/styles` split into focused partials; Phase 1 shell-vs-content restyle can now begin. | Branch `ui/design-foundation` |
| SF-119 | Done | UI | Phase 1 shell restyle | Shell, navigation, presentation mode, forms, dialogs, and responsive affordances refreshed on `ui/shell-and-navigation-refresh`. | Branch `ui/shell-and-navigation-refresh` |
| SF-120 | Done | UI | Phase 1 content restyle | Applied the new token layer to board, panel, drawer, scenario, and form surfaces in the content-views refresh. | Branch `ui/content-views-refresh`; commit `e900176`. |
| SF-121 | Done | UI | Panel review-cue CSS gap | Added matching panel.css rules for the review cue stack during the content-views refresh. | Branch `ui/content-views-refresh`; commit `e900176`. |
| SF-122 | Done | ML foundation | Simulated Trend Model (simulation-risk-ml-v0) shipped with synthetic-only training data, plain-JS logistic regression, and a checked-in model artifact. | Merged via PR #9 on 1 July 2026 after rebasing onto `codex/safeflow-prototype`. |
| SF-123 | Done | Backend safety gap | Local dev-server signal/suggestion providers (`createConfiguredSignalProvider`, `createConfiguredSuggestionProvider`) now refuse to silently serve placeholder fixtures when `SAFEFLOW_ENVIRONMENT` is not an allowed preview value, matching the guard already used by the Lambda handler (`allowsSimulationPreviewFallback`). Closes the "no silent placeholder fallback in production-intent environments" acceptance criterion from the delivery roadmap's DB-read-model backlog item. | `server/signalProvider.js`, `server/suggestionProvider.js`, plus new tests in `server/signalProvider.test.js` and `server/suggestionProvider.test.js`. Authored directly (Claude), not via Codex CLI, to conserve Codex usage. |
| SF-124 | Done | UI accessibility | WCAG AA accessibility pass across current UI surfaces: focus states, contrast, ARIA, keyboard navigation. | Merge commit `46ef3c0` via PR [#17](https://github.com/simionolimpiu-spec/wardsafe/pull/17). |
| SF-125 | Done | Dashboard data layer | Chart-ready aggregation service for ward comparison, simulated trend, and cue breakdowns. | Merge commit `f83d4bb` via PR [#21](https://github.com/simionolimpiu-spec/wardsafe/pull/21). |
| SF-126 | Done | Hospital insights dashboard | Hospital insights section with ward comparison and Patient Journey Twin simulated trend charts, using the shared chart-ready dashboard data service. | Squash merge commit `ccede12` via PR [#22](https://github.com/simionolimpiu-spec/wardsafe/pull/22). |
| SF-209 | Done | Data model | Fictional patient timeline + observations + review cues. | Merge commit `91c727c` via PR [#18](https://github.com/simionolimpiu-spec/wardsafe/pull/18); synthetic-only data only. |
| SF-211 | Done | Education | Clinical education, interprofessional learning, and portable competency passport | Simulation-only learning layer with micro-learning and structured courses tagged by ward/trust/profession/topic; squash merge commit `cf1577804876428723956b52e101cf0066b660af` via PR [#23](https://github.com/simionolimpiu-spec/wardsafe/pull/23). |
| SF-212 | Done | Simulation | More scenarios + safety flags | Merge commit `2abb2a04ce891bc390a132bb045535bce84a6653` via PR [#24](https://github.com/simionolimpiu-spec/wardsafe/pull/24); fictional ward scenarios and explainable safety cues only. |
| SF-213 | Done | Safety | Heuristic cue engine (explainable 'intuition') | Merge commit `fb6a9cef22158ad02f7a5e59bc94c68238d96621` via PR [#25](https://github.com/simionolimpiu-spec/wardsafe/pull/25); deterministic cue layer with transparent explanations and human review required. |
| SF-215 | Done | Safety / UI | Scope low-potassium flag and clean top nav | Merge commit `f63591d0e77dffc17611ad8ad25ef5038011ac1e` via PR [#26](https://github.com/simionolimpiu-spec/wardsafe/pull/26); scoped lab-signal flag wording to structured review support and refreshed navigation. |
| SF-214 | Done | ML | Extend simulation-risk ML model | Merge commit `ed4e5b60d1f150c5dfe258f4f52060887c8cb8e9` via PR [#27](https://github.com/simionolimpiu-spec/wardsafe/pull/27); synthetic-only model extension remains illustrative and not clinically validated. |
| SF-216 | Done | Data model | Ward simulation database | Squash merge commit `17971040696045291281b45c4d77d1120e72a724` via PR [#28](https://github.com/simionolimpiu-spec/wardsafe/pull/28); pure ward artifact rendering split out from the SQLite writer so CI can import the database fixtures safely. |
| SF-219 | Done | Governance | DTAC v2 self-assessment (working RAG, five domains) | `docs/public-demo-pack/build-readiness/dtac-v2-self-assessment.md`; honest gap register against the Feb 2026 DTAC form. Authored directly (Claude, docs-only), 3 July 2026. |
| SF-220 | Done (docs) | Evaluation | SF-210 evaluation framework: measures M1–M8, pre/post forms, SUS, facilitator sheet, data handling, pre-registered thresholds | `docs/public-demo-pack/evaluation-framework.md`. First real session still pending — see SF-210. Authored directly (Claude, docs-only), 3 July 2026. |
| SF-221 | Done | Research | NHS integration readiness research (closes SF-205): onboarding path, relevant APIs, UK Core FHIR/SNOMED implications, CIS2 seam design, ward-state negative finding | `docs/public-demo-pack/build-readiness/nhs-integration-readiness.md`; primary-source verified against digital.nhs.uk/developer on 3 July 2026. Research only; no integration authorised. |
| SF-223 | Done (docs) | Evaluation | First evaluation-session run plan: concrete, paternity-aware run-book for SafeFlow's FIRST simulation evaluation session (3–6 nurses/students; reads cues, generates a Ward Quality & Safety Review export SF-217, completes a Competency Passport module), operationalising the SF-210/SF-220 framework. Produces usability/usefulness evidence only, not clinical evidence. | `docs/public-demo-pack/build-readiness/first-evaluation-session-plan.md` (+ strategy-pack copy outside repo). Authored directly (Claude, Oli's-codex role, docs-only), 6 July 2026. Task O8. |
| SF-224 | Done (docs) | Quality reporting | SafeFlow Quality Intelligence stakeholder one-pager: single-page, plain-English description of the Ward Quality & Safety Review export (SF-217) pitched at Band 6/7/8a users (ward managers, practice educators, matrons, clinical governance leads), with a load-bearing "what it does not do" section (no staff scoring, no escalation-pathway start, does not replace EPR/RRT/Call-for-Concern or any live clinical/quality system, simulation-only). Every claim traceable to master-narrative.md. | Squash merge commit `7e9a126` via PR [#35](https://github.com/simionolimpiu-spec/wardsafe/pull/35); one new file `docs/public-demo-pack/quality-intelligence-one-pager.md` (+23/-0), no code/CONTROL.md changes in the feature PR; diff independently checked for banned wording (clean — Optica not named). Task O9. |
| SF-225 | Done (docs) | Scope discipline | Future Discovery Register: formally parks the three out-of-scope senior-nurse-feedback ideas — patient feedback stations, ward social hub, and cross-team discharge coordination (generically named; no competitor product named) — each with why-attractive / why-out-of-scope / open-governance-questions (framed as questions) / FUTURE DISCOVERY status. Points to the external IG/DPIA-lite checklist. Holds the simulation-only boundary and gives an honest "could SafeFlow also do X?" answer without building. | Squash merge commit `abcab4c` via PR [#36](https://github.com/simionolimpiu-spec/wardsafe/pull/36); one new file `docs/public-demo-pack/future-discovery-register.md` (+74/-0), no code/CONTROL.md changes in the feature PR; diff independently checked for banned wording (clean). Task O10. |
| SF-226 | Done (docs) | Quality reporting | Verified-learning-evidence narrative: adds the approved narrative home for a future ward-level verified-learning rollup (Mia's M10) to master-narrative.md — aggregated, NON-IDENTIFYING counts of completed simulation learning modules / Competency-Passport credits presented as "ward-level learning assurance (simulation)", human-review-framed, never individual staff scoring or person-level comparison. Adds approved terms ("verified learning evidence", "ward-level learning assurance (simulation)") and banned terms ("staff scoring", "competency ranking", "performance league table", "individual nurse rating"). | Squash merge commit `1c5e523` via PR [#37](https://github.com/simionolimpiu-spec/wardsafe/pull/37); one file `docs/public-demo-pack/master-narrative.md` (+11/-1); approved terms verified in the Approved Terminology list and banned terms in the Banned Wording list (correct placement independently confirmed); no code/CONTROL.md changes in the feature PR. Task O11. |
| SF-228 | Done | Scenarios | Deterioration-pattern review scenarios (Mia, M6): 3 fictional patterns — rising respiratory rate, new-onset confusion, falling oxygen saturation — that produce explainable, simulation-only, human-review-framed review cues through the existing signal engine. Each scenario carries success signals ("stays calm and explainable", "simulation-only and human-review required") and hazard guards (avoid treatment language / live-alert framing); signalEngine gains a guard regex blocking clinical wording. | Squash merge commit `28f54f0` via PR [#38](https://github.com/simionolimpiu-spec/wardsafe/pull/38); 3 files additive (`src/data/scenarioLibrary.js`, `src/domain/signalEngine.js`, `src/domain/signalEngine.test.js`, +184/-1), no server/infra/package/CONTROL.md changes in the feature PR; diff independently checked — banned-term matches were only the scenarios' own hazard labels (what to avoid) and the new guard regex, no banned usage; both CI checks green + CLEAN. From Mia's Codex. |
| SF-227 | Done | Safety QA | Safety-language scan coverage extended to the public demo pack markdown — the controlled wording source. `safetyLanguage.test.js` DOCUMENTATION_FILES now also scans master-narrative.md, quality-intelligence-one-pager.md, future-discovery-register.md, safety-boundary.md, stakeholder-demo-pack.md, demo-script.md, and build-readiness/first-evaluation-session-plan.md, so a future wording edit in the demo pack is caught by CI (closes the gap flagged when PR #29 covered only UI components). The scan found no banned wording to fix in those docs — confirming the Quality Intelligence docs merged this session are clean. | Squash merge commit `e80e5f5` via PR [#39](https://github.com/simionolimpiu-spec/wardsafe/pull/39); 1 file `src/domain/safetyLanguage.test.js` (+7/-0), test-only; both CI checks green + CLEAN; built fresh (the conflicting `pre-merge-safety-stash-2026-07-05` was left untouched). Task = Claude/Oli-codex safety-scan extension. |
| SF-127 | Done | Safety QA | Safety-language regression scan coverage extended to Twin/Hospital Insights/newer surfaces. | Squash merge commit `c0bbb8a` via PR [#29](https://github.com/simionolimpiu-spec/wardsafe/pull/29); adds `src/domain/safetyLanguageScans.test.js` and `src/components/safetyLanguageSurfaces.test.jsx` so future fixture/copy edits on these surfaces are caught automatically. |
| SF-229 | Done | Safety QA | Safety-language regression scans extended to Reports view and Ward Safety Board surfaces (renders real components incl. Quality Review drawer, scans rendered text with boundary-aware scanner). | Squash merge commit `1084791` via PR [#43](https://github.com/simionolimpiu-spec/wardsafe/pull/43); 1 test file extended (`src/components/safetyLanguageSurfaces.test.jsx`), zero production changes; both CI checks green. From Mia's Codex; QA'd and merged by orchestrator (Claude) 7 July 2026. |
| SF-230 | Done (docs) | Evaluation | In-session feedback capture sheet (O13) — quick-capture companion to the SF-220 evaluation framework, with explicit "framework wins" precedence note and cross-links to SF-220/SF-223. | Squash merge commit `793e92a` via PR [#42](https://github.com/simionolimpiu-spec/wardsafe/pull/42); `docs/public-demo-pack/build-readiness/evaluation-feedback-capture-sheet.md`. Precedence note added at merge time to prevent dual-instrument drift (HAZ-06/R-005). |
| SF-231 | Done | Safety QA | Ward Quality & Safety Review accessibility + review hardening (M9, from Mia's Codex): test coverage locking the export drawer/service behaviour and accessible review output. | Squash merge via PR [#47](https://github.com/simionolimpiu-spec/wardsafe/pull/47); `WardQualitySafetyReviewDrawer.test.jsx` + `wardQualitySafetyReviewService.test.js`; both CI checks green; test-only, no production or safety-guard changes. Verified + merged by orchestrator (Claude) 10 July 2026. |
| SF-232 | Done | Safety QA | Deterioration-scenario explainability lock (M11, from Mia's Codex): asserts each deterioration cue's visible copy explains WHY it flagged, stays non-alarming and human-review-framed, with a guard regex disallowing action/escalation/diagnosis wording. | Squash merge via PR [#46](https://github.com/simionolimpiu-spec/wardsafe/pull/46); `src/data/scenarioLibrary.js` + `scenarioLibrary.test.js`; both CI checks green; diff independently checked (banned matches were the guard regex only, not usage). Verified + merged by orchestrator (Claude) 10 July 2026. |
| SF-233 | Done (docs) | Governance | Staged information-governance checklist (delivers SF-206): simulation / evaluation / company / pilot stages with blocking gates surfaced (visa + IP checks before incorporation; DPIA before any real data). | Squash merge via PR [#45](https://github.com/simionolimpiu-spec/wardsafe/pull/45); `docs/public-demo-pack/build-readiness/ig-checklist.md`. Formal row added 10 July 2026 to close the phantom-ID gap (previously tracked only in the SF-206 note). |
| SF-234 | Done (docs) | Demo / Docs | Public simulation-preview deployment paths — clarifies how the simulation-only static build is published/reached (aligns docs with the GitHub Pages / static-host deploy work), simulation-only, no clinical claims. | Squash merge via PR [#50](https://github.com/simionolimpiu-spec/wardsafe/pull/50); `docs/public-demo-pack/build-readiness/public-simulation-preview.md` (+24/-16); banned-word scan clean; docs-only. Renumbered from Codex's proposed SF-234 collision-check by orchestrator (Claude) 10 July 2026. |
| SF-235 | Done (docs) | Evaluation | Evaluation-session facilitation script (O14): minute-by-minute read-aloud facilitator runbook for the first simulation evaluation session — participant-only consent, simulated-data boundary, the review-cues → Ward Quality & Safety Review export → Competency Passport sequence, SF-230 capture points, and debrief. Usability/usefulness evidence only, not clinical. Cross-refs SF-223 and SF-230. | Squash merge via PR [#51](https://github.com/simionolimpiu-spec/wardsafe/pull/51); `docs/public-demo-pack/build-readiness/evaluation-session-facilitation-script.md`. Rebuilt from Codex's unpushed loose worktree work; hardcoded SF-234 corrected to SF-235; safety scan 7/7. |
| SF-236 | Done (docs) | Demo / Docs | Live-site demo walkthrough (O15): click path + plain-English narration for driving the deployed static simulation preview in front of non-clinical (e.g. graphic designer) and Band 6/7 audiences, with the on-screen simulation-only boundary locations called out. Added to the safety-language scan and cross-linked from README/demo-script/demo-readiness. | Squash merge via PR [#51](https://github.com/simionolimpiu-spec/wardsafe/pull/51); `docs/public-demo-pack/live-site-demo-walkthrough.md` + scan/cross-link edits. Boundary "does not diagnose/prescribe" language confirmed allowed by the boundary-aware scanner. |
| SF-237 | Done | Quality reporting | Ward-level verified-learning rollup (M10) — adds an AGGREGATE, NON-IDENTIFYING "Ward-level learning assurance (simulation)" section to the Ward Quality & Safety Review export (service snapshot + export text + drawer UI), sourced from the competency passport (verified-credit + points counts) and micro-learning fixtures (module counts). No individual staff names, scores, ranking, or league table (R-002). Uses approved SF-226 wording. | Squash merge commit `b89c06a` via PR [#52](https://github.com/simionolimpiu-spec/wardsafe/pull/52); 3 files (`wardQualitySafetyReviewService.js`, `WardQualitySafetyReviewDrawer.jsx`, service test, +156/-1); test asserts aggregate counts, approved wording, and that NO fixture student name leaks into the export; safety scans + build pass. Authored directly by orchestrator (Claude) via desktop-commander, 10 July 2026. |
| SF-238 | Done (docs) | Demo / Docs | Demo-pack index & reading order (O16) — `pack-index.md` groups the now-large demo pack by audience (start-here / stakeholder / evaluation / governance / build-readiness), reaffirms master-narrative.md as the controlled wording source, and is added to the safety-language scan. | Squash merge commit `e23f5a7` via PR [#53](https://github.com/simionolimpiu-spec/wardsafe/pull/53); `docs/public-demo-pack/pack-index.md` + 1 line in `safetyLanguage.test.js` DOCUMENTATION_FILES; safety scan 7/7. Authored directly by orchestrator (Claude), 10 July 2026. |
| SF-239 | Done | Demo / UX | Presentation / demo mode (M12, extends existing SF-004 Presentation Mode) — toggle-able presentation mode with larger token-based legibility and a 4-step guided flow (review cues -> Ward Quality & Safety Review export -> Competency Passport -> Hospital Insights); opens the existing export drawer on its step; simulation-only banner stays visible throughout; keyboard-accessible (aria-pressed). | Squash merge via PR [#54](https://github.com/simionolimpiu-spec/wardsafe/pull/54); 3 files (`src/App.jsx`, `src/App.test.jsx`, `src/styles/shell.css`, ~144 lines); no data/package/server/infra/CI changes; banned-word scan clean; both CI checks green. From Codex; verified + merged by orchestrator (Claude) 10 July 2026 (Codex proposed SF-237 but that was assigned to M10; renumbered to SF-239). |
| SF-240 | Done (docs) | Demo / Docs | Live demo URL wired into the pack (O17) — README, pack-index, live-site-demo-walkthrough, and public-simulation-preview now reference the live public build https://simionolimpiu-spec.github.io/wardsafe/ (each with a simulation-only / not-for-clinical-use note), and record that it auto-deploys on push to codex/safeflow-prototype. | Squash merge via PR [#55](https://github.com/simionolimpiu-spec/wardsafe/pull/55); 4 docs (+13/-1); banned-word scan clean; both CI checks green. Live site verified HTTP 200 with JS bundle loading before wiring. From Codex; verified + merged by orchestrator (Claude) 12 July 2026. |
| SF-241 | Done | Data / Simulation | Two-trust simulation patient database (`src/data/trustNetwork/`). Real trust + ward NAMES only (James Paget = EXACT 33-ward directory from jpaget.nhs.uk, July 2026; Norfolk & Norwich = sourced 18-ward subset from nnuh.nhs.uk Wards A-Z, July 2026, flagged to re-verify); all PATIENTS and journeys FICTIONAL and non-identifying. Models inter-trust handover: James Paget -> Norfolk & Norwich specialist care -> health-plan continuation -> return to James Paget OR packages of care / discharge (4 journeys, 20 segments). Simulation-only, human-review-framed; NOT a live inter-trust record or coordination system. FK-integrity-checked; CLI generator emits CSV + manifest (no node:sqlite). | Squash merge commit `b515b89` via PR [#57](https://github.com/simionolimpiu-spec/wardsafe/pull/57); new module `trusts.js`/`index.js`/`trustNetworkArtifacts.js`/`trustNetwork.test.js` + `scripts/build-trust-db.mjs` + generated `data/exports/two-trust/*`; 7 module tests + 17 safety-scan tests + build all green; additive, not yet wired into UI (proposed follow-up). Authored directly by orchestrator (Claude), 12 July 2026. |
| SF-242 | Done | Data / UX | Two-Trust Network view — read-only UI (`src/components/TrustNetworkView.jsx`) rendering the SF-241 data on the live site: the two trusts with ward + fictional-patient counts, and the inter-trust handover journeys (trust chips + ward + stage + handover note + outcome badge) so the handover between James Paget and Norfolk & Norwich and the continuing health plan are visible. Simulation-only banner unchanged; real ward names, fictional patients, not a live inter-trust system. | Squash merge commit `2162542` via PR [#58](https://github.com/simionolimpiu-spec/wardsafe/pull/58); new view + test + `trust-network.css`, a 'Two-Trust Network' tab + render branch in `App.jsx`, added to the boundary-safe safety-language surface scan; 69/69 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-243 | Done | Data / Simulation | England trust network — expands SF-241 from 2 to **6 trusts**: James Paget (EXACT 33-ward directory), Norfolk & Norwich + Addenbrooke's/CUH (sourced real wards, inc. NNUH Buxton/Cringleford/Holt AMU and CUH neurosciences A3-A5 / transplant F5/G5), plus West Suffolk, Ipswich (ESNEFT) and QEH King's Lynn (representative real FUNCTIONAL units, labelled per-trust via `wardSource`). 99 wards, 209 fictional non-identifying patients (each with a `homeTrustId`). Simulation-only. | Squash merge commit `366f92d` via PR [#59](https://github.com/simionolimpiu-spec/wardsafe/pull/59); rewrote `trusts.js`/`index.js`/artifacts + regenerated `data/exports/two-trust/*` (now inc. `learning_records`); FK-integrity OK; 75/75 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. NNUH/representative ward completeness flagged to re-verify. |
| SF-244 | Done | Data / UX / Governance | Portable cross-trust patient journey + **learning-copy-to-origin** — patients carry a home trust; journeys model relocation / temporary-visitor (found away from home) / specialist-transfer / inter-trust-handover with the health plan continuing and the patient returning home or moving to packages of care / discharge; every journey emits a `learningRecord` returned to the ORIGINATING trust with teaching points (the teaching feedback loop). Concept + honest IG boundary in `portable-patient-journey-concept.md` (live version = regulated Shared Care Record / DPIA future discovery, NOT a live record). Trust Network view + generated CSVs render it. | Squash merge via PR [#59](https://github.com/simionolimpiu-spec/wardsafe/pull/59); view/test updates, concept doc added to safety scan, `learning_records` CSV table; boundary-safe surface scan + 75/75 tests + build green. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-245 | Done | Data / UX | Per-patient fictional observation timelines (`patientTimelines.js`) — each of the 209 fictional trust-network patients gets a deterministic 4-point NEWS2-style sequence (resp/SpO2/HR/systolic BP/temp/consciousness) seeded from its id; drifts only for observation-trend / escalation-readiness themes (explainable review cue), otherwise stable; trend note is review-support framing, not a clinical score. Journeys carry a fictional patient subject; the Trust Network view renders that patient's moving observation strip in each journey card. New `observations` CSV table (836 obs). Concept doc gains an 'Alignment, not replacement' section (aligns with Shared Care Record / GP Connect, not a duplicate). | Squash merge commit `30423dd` via PR [#60](https://github.com/simionolimpiu-spec/wardsafe/pull/60); `patientTimelines.js`+test, index/artifacts/view updates, css; 23/23 tests + generator + build green; FK-integrity OK. Authored directly by orchestrator (Claude), 12 July 2026. Auto-deploys to the live site on merge. |
| SF-246 | Done | Data / Simulation | Real sourced ward sets for the three district generals — replaces the representative placeholders: **West Suffolk** (F7/F9/F10, G3/G4/G8, E17 + ED/AMU/ICU), **Ipswich/ESNEFT** (27 real wards from the full esneft.nhs.uk Ipswich Wards A-Z: Brantham, Brook, Sproughton, Bergholt … Woodbridge, Framlingham NICU), **QEH King's Lynn** (Windsor/Necton/Elm + Tilney/Gayton/Denver/… + Castleacre maternity). Five of six trusts now carry real ward names (James Paget exact; the rest sourced). Cross-region journey ward ids updated to the new real wards. Now 126 wards, 293 fictional patients, 1,172 observations. | Squash merge commit `4bcc12d` via PR [#61](https://github.com/simionolimpiu-spec/wardsafe/pull/61); rewrote WSH/ESNEFT/QEH ward arrays in `trusts.js`, wardSource representative→sourced, updated journey segment ids in `index.js`, regenerated exports; 10/10 tests + generator + build green; FK-integrity OK. Authored directly by orchestrator (Claude), 12 July 2026. Each trust flagged partial/re-verify for full completeness. |

## Started / Open / Ready

| ID | Status | Area | Work item | Next action |
|---|---|---|---|---|
| SF-101 | Closed | Release | PR #7 feature/minimal-role-aware-gui closed as superseded. | No merge planned. |
| SF-104 | Done | AWS architecture | Mock/readiness AWS direction documented in `docs/public-demo-pack/build-readiness/architecture-options.md` and `docs/public-demo-pack/build-readiness/technology-stack.md` (Aurora, Step Functions, Bedrock/LLM draft provider, tokenised backend, RBAC/audit logging). | Merged via [PR #19](https://github.com/simionolimpiu-spec/wardsafe/pull/19) on 2 July 2026; squash merge commit `5384b54b7f71d61b9b614df7c753255edab39d18`. Keep as mock/readiness architecture only; no live AWS deployment or SDK wiring until explicit approval. |
| SF-105 | Done | Docs alignment | `docs/public-demo-pack/master-narrative.md` is now the controlled description; `stakeholder-demo-pack.md`, `demo-script.md`, `safety-boundary.md`, and `README.md` are aligned to it. | Merged via PR [#44](https://github.com/simionolimpiu-spec/wardsafe/pull/44), squash commit `f7c918f`, 7 July 2026 (conflicts with #37/#40/#41 resolved in favour of newer base wording — the QI-lane master-narrative with SF-226 terms is the surviving version). App-copy naming cleanup remains a Mia follow-up. |
| SF-107 | Done | Clinical safety | Simulation twin / learning idea consolidated as a bounded, simulation-only, review-support + education concept (fictional Patient Journey Twin over fictional timelines feeding the learning / Competency-Passport loop; never a real-patient twin, predictor, or clinical action). | Concept recorded in `docs/public-demo-pack/build-readiness/simulation-twin-learning-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)); review-support and education only. |
| SF-109 | Done | Governance | Clinical safety case outline and hazard log skeleton drafted. | `docs/public-demo-pack/clinical-safety-case-outline.md`, using `templates/safety-case-outline-template.md`; 10-row hazard log grounded in the actual signal-envelope contract (`signalClient.js`) and existing risks R-001â€“R-008. Not yet independently reviewed; roles unfilled pending a future pilot. |

## Quality Intelligence lane (5 July 2026, from senior-nurse feedback pressure-test)

Senior-nurse feedback (Band 6/7 quality reports, deteriorating-patient review, RRT/Call-for-Concern, patient feedback stations, social hub, Optica-style discharge coordination) was pressure-tested against the simulation-only boundary; full memo saved outside this repo as `SafeFlow_Senior_Nurse_Feedback_Strategy_Memo_2026-07-05.md`. Verdict: patient feedback stations and a social hub stay out as future discovery (real IG/consent gap, SF-206 still backlog); Optica-style discharge coordination and naming a real competitor product stay out of external material entirely. The two in-scope items below are dispatched as SF-217 (Oli) and SF-218 (Mia).

| ID | Status | Area | Work item | Next action |
|---|---|---|---|---|
| SF-217 | Done | Quality reporting | Ward Quality & Safety Review export — assembles heuristic cue engine flags, simulation-risk trend summary, and Competency Passport verified-learning evidence into one Band-6/7-styled exportable report, reusing the existing Simulation Review Report export pattern. | Squash merge commit `8e1dcbb` via PR [#31](https://github.com/simionolimpiu-spec/wardsafe/pull/31); 9 files (`src/App.jsx`, `src/App.test.jsx`, `src/components/ReportsView.jsx`, new `WardQualitySafetyReviewDrawer.jsx`+test, extended `safetyLanguageSurfaces.test.jsx`/`safetyLanguageScans.test.js`, new `wardQualitySafetyReviewService.js`+test); post-merge `npm test` 70 files/400 tests passed, `npm run build` passed; diff independently checked for banned wording (clean — only approved terms like "escalation readiness cue" and the standard boundary phrase appear). No server/infra/package.json/CONTROL.md changes. |
| SF-218 | Done | Education | Deterioration + escalation-pathway learning modules — 2–3 new Learning Hub micro-learning modules covering deteriorating-patient recognition and RRT-call/Call-for-Concern conversation structure, education only, no live escalation logic. | Squash merge via PR [#48](https://github.com/simionolimpiu-spec/wardsafe/pull/48) (M7, from Mia's Codex); adds `src/data/microLearningFixtures.js` content + LearningHubView/drawer/service/safety-scan tests; both CI checks green; diff independently checked (no banned usage — only guard patterns). Verified + merged by orchestrator (Claude) 10 July 2026. |

## Backlog

| ID | Status | Area | Work item | Notes |
|---|---|---|---|---|
| SF-201 | Done | UI | Full-screen / presentation mode — delivered by the M12 presentation mode (SF-239): toggle-able presentation mode with larger token-based legibility, a 4-step guided flow, and the simulation-only banner always visible. | Closed 12 July 2026 as delivered by SF-239 (PR [#54](https://github.com/simionolimpiu-spec/wardsafe/pull/54)). Any further full-screen polish is optional and would land as additive token CSS. |
| SF-202 | Done | UI | Cleaner NHS-realistic v2 visual concept — calm / clinical-grade / accessible design direction (colour roles, type & spacing, component notes) for a graphic designer to build on; token-driven, additive, no behaviour or boundary change. | Concept in `docs/public-demo-pack/build-readiness/nhs-realistic-v2-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Any resulting restyle lands as additive token CSS after designer input. |
| SF-204 | Closed (superseded) | AWS docs | AWS mock docs and architecture diagrams | Delivered by SF-104 (PR #19, `docs/public-demo-pack/build-readiness/architecture-options.md` + `technology-stack.md`). No further action. |
| SF-205 | Closed (delivered) | Research | NHS Digital API research only | Delivered by SF-221 (`nhs-integration-readiness.md`). |
| SF-206 | Closed (delivered) | Governance | IG checklist | Delivered by SF-233 (`docs/public-demo-pack/build-readiness/ig-checklist.md`, PR [#45](https://github.com/simionolimpiu-spec/wardsafe/pull/45)): staged checklist (simulation / evaluation / company / pilot) with blocking gates surfaced (visa + IP checks before incorporation; DPIA before any real data). SF-231/SF-232 reserved for Mia's pending branches; SF-218 fulfilled by her escalation-pathway-learning branch when it lands. |
| SF-207 | Done | Security | RBAC + audit-logging concept/readiness — least-privilege role model + append-only, tamper-evident audit design for a FUTURE governed pilot only; nothing built or enabled now (no real data, R-003). | Concept in `docs/public-demo-pack/build-readiness/rbac-audit-logging-concept.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Downstream of the clinical safety case + IG checklist; recorded as design/readiness, not authorisation to implement. |
| SF-210 | Ready to run | Evaluation | Demo evaluation framework — the full kit is complete and consistent (framework SF-220, run plan SF-223, facilitator script SF-235, capture sheet SF-230, plus a live demo to evaluate). | Readiness summary in `docs/public-demo-pack/build-readiness/evaluation-readiness-summary.md` (PR [#56](https://github.com/simionolimpiu-spec/wardsafe/pull/56)). Only remaining step is operational: schedule + recruit 3–6 participants, run the session, write the first report in `docs/evaluation-reports/`. |

## Risks and controls

| ID | Risk | Control |
|---|---|---|
| R-001 | Project sounds like a live clinical decision tool. | Use simulation-only, education, human review, and review-support wording everywhere. |
| R-002 | Scope creep into prescribing, treatment advice, or automated escalation. | Avoid live-clinical action claims and keep the review-support boundary explicit. |
| R-003 | AWS deploy risk or accidental cost. | Keep SAFEFLOW_DEPLOYMENT_APPROVED false (explicitly set, not merely unset) unless explicitly preparing controlled simulation deployment. |
| R-004 | Real patient data accidentally enters demo material. | Use fictional patients, fictional observations, and sanitised examples only. |
| R-005 | Docs, slides, and app language drift apart. | Update this control board after each change; treat exported chat docs as history, not live truth. |
| R-006 | Public preview shared before release review. | Confirm PR status, CI, branch cleanliness, and wording before merge/tag. |
| R-007 | LLM/digital twin wording creates unsafe expectations. | Call it Patient Journey Twin / Simulation Patient Twin and explain that it supports learning and review, not autonomous care. |
| R-008 | Control docs reference the wrong PR number after external export/import. | Always re-pull live git/GitHub state before updating this file; do not trust prior chat summaries alone. |
| R-009 | Simulated ML trend output (`patientJourneyTrendModel.js`, weights in `patientJourneyTrendModel.json`) gets read as a validated clinical result. | Keep "Illustrative model output, not clinically validated" on every rendered trend chart/summary; never surface `testMetrics` (accuracy/precision/recall) in the UI â€” confirmed today they are data-layer only, not rendered. See SF-127 for closing the automated-scan gap on this and the Twin/Hospital Insights surfaces. |
| R-010 | Next-phase scope drifts into live or black-box claims. | Keep this phase simulation-only, with no real-patient data, no autonomous clinical action, no AGI/black-box language, and ship each item as its own scoped PR. |

## Core decisions to preserve

- SafeFlow / WardSafe remains simulation-only unless governance and deployment approval change.
- No real patient data is used in the demo or project materials.
- No live NHS integrations are included at this stage.
- No AWS SDK/live deployment should occur unless explicitly approved.
- All clinical language must remain human-review focused.
- Digital twin language should be controlled as Patient Journey Twin or Simulation Patient Twin.
- This file (CONTROL.md) is now the source of truth for what has been done, what is open, and what happens next; update it after every commit, doc change, or PR event.

## Recommended next actions

| Priority | Action |
|---|---|
| 1 | Record PR #7 as closed as superseded and keep the tracker aligned with GitHub. (Done) |
| 2 | Draft a clinical safety case outline and hazard log skeleton. (Done â€” see SF-109) |
| 3 | Keep the public docs aligned with the master narrative and raise the remaining app-copy naming cleanup as a Mia task. |
| 4 | Decide the next build focus: Patient Journey Twin UI, AWS mock architecture, or full-screen presentation polish. |
| 5 | Get the clinical safety case outline (SF-109) independently reviewed once a clinical safety lead is identified. |
| 6 | Keep this file updated after every commit, document, or design decision. |

## Changelog

- ID reconciliation note: SF-217 stayed with the Ward Quality & Safety Review export; the governance docs moved SF-217→SF-219, SF-218→SF-220, and SF-219→SF-221. The doc-only SNOMED/FHIR placeholder was advanced to SF-222 so it stays clear of the new board IDs.

## Authoring note (1 July 2026)

This safety-case work (SF-109) and the roadmap refresh were authored directly against the
repository files rather than via a Codex CLI session, to conserve Codex usage for code changes
that need build/test verification. Docs-only changes like this one carry no build risk, so direct
authoring plus a short terminal-only commit/push/PR step is the lighter-weight path going forward
for non-code control-board and documentation work.

## PR #14 scope-creep finding (1 July 2026, for Mia)

PR #14 (`patient-journey-twin`) is **not ready to merge**. A disposable, unpushed local rebase
onto the latest `origin/codex/safeflow-prototype` was used to separate genuine branch content
from stale-base noise. After rebasing, the branch still shows 88 changed files and roughly
15.6k/5.8k line insertions/deletions against current base, and the expected `WorkspaceNav.jsx`
wiring change is not present at all. This is real scope creep, not a rebase artifact. Recommended
fix: start a clean branch from current `codex/safeflow-prototype` and re-apply just the new
Patient Journey Twin component(s), their domain/test files, and the intended `App.jsx` /
`WorkspaceNav.jsx` wiring â€” nothing else. See SF-102 above for the full file list this ruled out.
| SF-247 | Done | UX | Ward-first demo scenario selector (M13) - replaces the flat combinatorial "Ward x review type" dropdown with two-step selection: a **Ward** select (Gastro, AMU, Day Care, Surgical, Paediatrics, Community, General Medical, Acute Medical - derived from the ward library, not hard-coded) plus a short **Review focus** select (Documentation, Discharge readiness, Escalation readiness, Medicine timing, Observation trend, .). New `getWardOptions()` / `getReviewFocusOptions(wardId)` / `resolveScenarioId(wardId, focusId)` helpers in `wardLibrary/index.js` and `getDemoScenarioSelectionOptions()` in `demoScenarios.js`; every existing scenario id round-trips unchanged, so downstream scenario data is untouched. Simulation-only note kept on both selects. | Squash merge commit `cdcd950` via PR [#62](https://github.com/simionolimpiu-spec/wardsafe/pull/62); DemoScenarioSelector rework + component test, wardLibrary + demoScenarios helpers + tests, App wiring; CI green (both checks); safety grep clean. Built by Codex (Mia lane) from the 17 Jul dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-248 | Done | UX / Design | O18a app shell redesign - the approved "SafeFlow Nursing / Ward Safety Board" visual direction applied app-wide: deep-blue top bar with plain blue **SF logomark (deliberately NOT the NHS logo - trademark boundary)**, "SafeFlow Nursing" + view subtitle, ward-first scenario selector + steppable simulation date in the header, notification bell + "Fictional Nurse - Charge Nurse" chip; white left sidebar (`WorkspaceNav`) with icons + Tasks/Escalations badges covering all 16 views + a "Safety first - report concern" card; KPI summary-card row (`BoardSummaryCards`: patients / escalations / NEWS2>=5 / handover % / discharge-ready) computed from existing simulation data; new design tokens (`--sf-blue` family, WCAG-AA risk colours) applied across all view stylesheets. Simulation-only banner kept on every view (Hospital Insights renders its own). No realistic names/DOB/NHS-number fields introduced. | Squash merge commit `9adaf34` via PR [#63](https://github.com/simionolimpiu-spec/wardsafe/pull/63); AppShell/WorkspaceNav/BoardSummaryCards/SafetyBanner + tests, 13 stylesheets, App/e2e updates. Initial CI failure (e2e asserted the old `.metric-grid` layout) fixed by orchestrator: merged M13 into the branch, made the top bar host the ward-first DemoScenarioSelector, dropped the redundant WardContextSelector, updated the e2e journey; then CI green (both checks) + local vitest 432 + Playwright 2/2. Built by Codex (Oli lane), reconciled + merged by orchestrator (Claude), 17 July 2026. |
| SF-249 | Done | UX / Design | O18b board table + detail panel restyle - the Ward Safety Board patient table restyled to the approved direction (uppercase column headers, row hover/selected states with blue accent, risk chips readable as text + colour, link-button styling) and the `PatientSafetyPanel` right-hand detail drawer restyled with tabbed SBAR / Tasks / Audit sections per the mock, keeping the existing human-review wording (no "escalate now"-style automated language added). Safety-language surface scan extended over the restyled components. | Squash merge commit `14efb62` via PR [#64](https://github.com/simionolimpiu-spec/wardsafe/pull/64); WardSafetyBoard + PatientSafetyPanel + tests, board/panel css. Orchestrator merged the O18a-squash base into the stacked branch (kept patient-table additions, took base for shell files), retargeted the PR base, local vitest 433 + Playwright 2/2 + CI green before merge. Dead-code note: the stack resurrected the unused WardContextSelector files; deleted in the SF-247..249 records commit. Built by Codex (Oli lane), reconciled + merged by orchestrator (Claude), 17 July 2026. |
| SF-251 | Done | Data / Domain | Longitudinal patient journey engine (L1) - day-indexed deterministic model on top of the SF-245 seeding pattern: `getPatientDay(patientId, dayNumber)` (pure, cost-independent of day number - day 1300 as cheap as day 1), `buildEpisodes(patientId, throughDay)` (admission -> ward moves -> inter-trust transfer -> discharge -> community -> possible readmission, reusing the SF-243 portable-journey definitions), `compareDays(patientId, dayA, dayB)` (then-vs-now delta: per-observation change, location change, phase change, review-support-framed trendNote). Additive `buildLongitudinalPatientTimeline()` bridge into the existing Twin data source; current fixtures/tests untouched. | Squash merge commit `6c4dec3` via PR [#65](https://github.com/simionolimpiu-spec/wardsafe/pull/65); `longitudinalJourney.js`+test, `patientTimeline.js` bridge; determinism incl. day 1300, episode ordering, compareDays symmetry, perf sanity, no name/DOB/nhsNumber, banned-wording scan all covered; CI green (both checks). Built by Codex (Mia lane) from the 17 Jul longitudinal-phase dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-252 | Done | UX | Patient Journey Twin longitudinal scrubber + then-vs-now (L2) - Twin gains a day scrubber (accessible slider + prev/next, `aria-valuetext` "Day N of M", supports long journeys e.g. day 1300 without label breakage), an episode chapter strip (admission/ward/transfer/discharge/community/readmission - clicking jumps the scrubber, inter-trust segments use the Trust Network chip style), a "then vs now" panel driven by `compareDays()` (per-observation delta rows with direction + plain-language review-support note, location then/now, phase then/now), and an inline SVG trend sparkline per observation (no chart library, colour-blind-safe, values also readable as text). Default view = latest day; selected day persists across patients only while valid. | Squash merge commit `3839d6f` via PR [#66](https://github.com/simionolimpiu-spec/wardsafe/pull/66), stacked directly on the SF-251 commit (identical engine file, zero-diff on shared files - verified before merge); `PatientJourneyTwin.jsx` rework + test, `scenario-and-audit.css` additions, safety-surface scan extended. CI green (both checks); local re-verification post-merge: 441/441 vitest, Playwright 2/2, build green. Built by Codex (Oli lane), verified + merged by orchestrator (Claude), 17 July 2026. |
| SF-250 | Done | UX / Design | Visual fidelity pass on the O18 redesign - fixed the root cause of the font mismatch (tokens.css declared Inter but nothing ever loaded it; self-hosted `@fontsource/inter` 400/500/600/700/800 now imported in `main.jsx`, e2e now asserts computed `font-family` actually contains Inter); fixed the date stepper (previously changed the visible label but left `<time dateTime="2026-06-17">` hardcoded - now tracks the real displayed date via `toDateTimeValue()`); replaced the dead notification bell with a working "Simulation notifications" popover (honest "No simulation notifications recorded" placeholder, proper aria-expanded/aria-controls); removed the duplicate navigation (the old horizontal `tab-list`/"Prototype journey" strip that still drove view-switching underneath the new sidebar) so WorkspaceNav is the single source of navigation, with `e2e/safeflow.spec.js` rewritten to drive all views through the sidebar (all prior safety assertions - banner, no "NHS" text, potassium safety-gap flow, SBAR draft save, scenario library - kept intact); removed dead `.metric-grid` CSS. Orchestrator note: a suspected "NEWS2 ≥5" encoding bug flagged in the dispatch prompt was investigated post-PR and found to be a false positive (PowerShell console codepage artefact, not a file corruption) - confirmed via direct Node codepoint read (U+2265, correct); no fix was needed or applied, and the label was left untouched. | Squash merge commit `f9f330e` via PR [#67](https://github.com/simionolimpiu-spec/wardsafe/pull/67); `AppShell`+test, `App.jsx`+test, `WardSafetyBoard.jsx`, `main.jsx`, board/forms-and-dialogs/responsive/shell css, `package.json` (+@fontsource/inter), e2e rewrite. CI green (both checks); local re-verification post-merge: 443/443 vitest, Playwright 2/2 (desktop+mobile), build green, safety grep clean (no nhsNumber/DOB/NHS-No/NHS-logo). Built by Codex (Oli lane) from the 17 Jul fidelity-pass dispatch prompt, verified + merged by orchestrator (Claude), 17 July 2026. |

| SF-253 | Done | Data / Infra | AWS/DB longitudinal scale-up (L3) - `journey_episodes` and `longitudinal_observations` tables added to `database/schema.sql`, keyed on `(patient_id, day_number)` with 4 range-query indexes; deliberately not FK'd into `patient_summaries` (that table uses the older `DCU-XXX` ward-simulation namespace, these use the SF-243/245 trust-network `JPUH-P-001`-style namespace - documented in SQL comments to prevent future conflation). `src/domain/longitudinalMaterialization.js` (pure row/CSV mapping) + CLI-only `scripts/build-longitudinal-db.mjs` (SF-216 lesson: no vitest/node:sqlite contamination) materialise the SF-251 engine's output at scale: 293 patients x 1300 days = 380,900 patient-days / 2,285,400 observation rows in 27.2s (~84k rows/sec) - measured as generation+serialisation only, since no live Postgres instance is reachable in this dev/CI environment; live query-latency proof against a provisioned database is deferred to the existing `SAFEFLOW_DEPLOYMENT_APPROVED` gate (R-003), not fabricated here. Three new read-only GET endpoints (`/api/simulation/longitudinal/patients/:id/days/:day`, `/episodes`, `/compare`) added identically to `server/api.js` and `infra/aws/lambda/safeflowApi/index.mjs` - no DB round-trip needed since the engine is deterministic and O(1) per day. `database/migrationApproval.json` updated (schema hash) through the existing human-gate migration runner, no bypass. Committed `manifest.json` reflects the smaller 60-day default sample (17,580 patient-days / 105,480 observations); large CSV exports gitignored. | Squash merge commit `0072475` via PR [#68](https://github.com/simionolimpiu-spec/wardsafe/pull/68); `database/schema.sql`+test, `migrationApproval.json`, `longitudinalMaterialization.js`+test, `build-longitudinal-db.mjs`, `server/api.js`+test, `infra/aws/lambda/safeflowApi/index.mjs`+test, `.gitignore`, `package.json` scripts, `data/exports/longitudinal/manifest.json`. CI green (both checks); local re-verification post-merge: 79/79 vitest files, 460/460 tests, build green, `db:manifest`/`db:migrate:plan` match approval, `infra:synth:dev`/`infra:synth:simulation` clean, `api:smoke` 7/7, Playwright 2/2, safety grep clean (no nhsNumber/DOB/NHS-No/name). Built and merged directly by orchestrator (Claude), 17 July 2026. |

| SF-254 | Done | Governance / IG | Longitudinal-record IG boundary doc (L4) - `docs/public-demo-pack/build-readiness/longitudinal-record-boundary.md`, mirroring the structure of the SF-245 portable-journey concept doc: distinguishes the simulated/generated longitudinal data built in SF-251/252/253 from what a future LIVE captured longitudinal record would require - a DPIA scoped specifically to longitudinal (not single-snapshot) processing, Article 6/9 lawful basis kept current as the record grows, a DCB0129/0160 safety-case rescope for trend-over-time review (staleness/drift/over-trust-in-an-old-trend failure modes a single-snapshot safety case doesn't cover), DSPT currency, per-trust data-sharing agreements, and a retention section specific to long records (per-data-point retention timers, partial erasure/rectification mid-record, per-time-slice treatment of the existing learning-copy pattern). Same "alignment, not replacement" posture vs Shared Care Record/GP Connect as SF-245. Live longitudinal capture stays future discovery, gated behind this doc plus `ig-checklist.md`. Doc added to the safety-language scan file list (`safetyLanguage.test.js`); passed cleanly on first run. | Commit `fe900c5` on `codex/safeflow-prototype` (docs-only, direct); new doc + `safetyLanguage.test.js` DOCUMENTATION_FILES addition. Local verification: 79/79 vitest files, 460/460 tests (incl. the extended safety-language scan), build green, diff grep clean (no nhsNumber/DOB/NHS-No/competitor names/automated-escalation wording). Written and pushed directly by orchestrator (Claude), 17 July 2026. |
